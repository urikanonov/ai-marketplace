# Tests (not distributed)

Put this plugin's tests here (unit, end-to-end, fixtures, and any test config). Nothing under `dev/` is
shipped to users. Wire a CI job scoped to this plugin's path to run them.
