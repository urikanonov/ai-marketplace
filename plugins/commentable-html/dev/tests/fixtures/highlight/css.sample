/* Theme tokens and layout */
:root {
  --gap: 8px;
  --fg: #1f2328;
}

.card {
  display: flex;
  gap: var(--gap);
  margin: 0 auto;
  color: var(--fg);
  border: none;
}

.card:hover {
  transform: scale(1.02);
}
