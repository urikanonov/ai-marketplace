#include <iostream>
#include <vector>

// Sum a vector of ints
int sum(const std::vector<int>& xs) {
    int total = 0;
    for (auto x : xs) {
        total += x;
    }
    return total; /* accumulate */
}

int main() {
    std::vector<int> xs = {1, 2, 3};
    std::cout << sum(xs) << "\n";
}
