// Greeting helper
fun greet(name: String): String {
    return "Hello, $name!"
}

fun main() {
    val names = listOf("Ada", "Grace")
    for (name in names) {
        /* one line each */
        println(greet(name))
    }
    val count = 42
    println("count = $count")
}
