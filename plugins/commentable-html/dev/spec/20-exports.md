## Copy-all bundle and handled-id contracts

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-COPY-01 | Copy all emits a Markdown bundle with document label, `Source`, ordered comments, agent instructions, and a final `HANDLED_IDS_JSON` array. | `tests/05-copy-handled.spec.js` - `Copy all emits the markdown bundle with an exact HANDLED_IDS_JSON line`; `tests/12-features.spec.js` - full text bundle contract |
| CMH-COPY-02 | Text-comment bundle entries include `Where`, `Pinpoint`, offsets, quoted text, optional context, containing block, and the user's note. | `tests/12-features.spec.js` - full text bundle contract and heading metadata tests |
| CMH-COPY-03 | Copy all uses the same bundle from toolbar and sidebar, and it has anchor-specific payloads for code, mermaid, diff, and image comments. | `tests/12-features.spec.js` - toolbar Copy all, code and mermaid copy tests; `tests/13-diff.spec.js` - diff copy tests; `tests/14-image.spec.js` - image copy test |
| CMH-COPY-04 | Copy all with no copyable comments or tracked state changes is disabled, copies nothing, and exposes a `No comments to copy` tooltip instead of running the copy path. | `tests/12-features.spec.js` - `Copy all is disabled with zero comments and re-enabled after a comment exists (CMH-COPY-04, CMH-COPY-07)` |
| CMH-COPY-05 | Copy all clipboard writes use `navigator.clipboard` first, then `execCommand`, then a `prompt` fallback. When the user cancels the prompt the toast must not claim success - it must describe what actually happened. | `tests/12-features.spec.js` - `Copy all falls back to execCommand when the async clipboard is unavailable`, `Copy all falls back to prompt() when both clipboard paths fail`; `tests/36-a11y.spec.js` - `Copy all does NOT claim success when it falls back to a manual prompt` |
| CMH-COPY-06 | Generic per-block clipboard actions (code-block Copy buttons and `data-cmh-copy` chips) use `navigator.clipboard` then `execCommand`; if both fail they show a `Copy failed` toast and do NOT open a `prompt` (only Copy all has the prompt fallback). | `tests/38-spec-gaps.spec.js` - `per-block copy failures show Copy failed without opening prompt (CMH-COPY-06)`; source: `assets/js/` - code-block copy and `data-cmh-copy` handlers |
| CMH-COPY-07 | Copy all controls expose `aria-disabled="true"` and the tooltip `No comments to copy` when there is no copyable review state, stay keyboard-focusable for the same tooltip, re-enable once a comment exists, and return to disabled after Clear removes the last comment. | `tests/12-features.spec.js` - `Copy all is disabled with zero comments and re-enabled after a comment exists (CMH-COPY-04, CMH-COPY-07)`; source: `assets/js/` - `updateCopyAllState()` |
| CMH-COPY-08 | Copy all treats reviewer notes and the document source as untrusted: each free-text note is wrapped verbatim in a dynamic, tilde-sized `UNTRUSTED REVIEWER NOTE` fence (longer than any tilde run in the note), a read-first AGENT INSTRUCTIONS block frames notes as document-scoped change requests (data, not agent/system instructions), and `data-doc-source` is emitted single-line with backticks neutralized so an embedded newline cannot forge a standalone machine line and a backtick cannot close the Markdown code span it sits in. | `tests/05-copy-handled.spec.js` - `forged trailer/instruction lines in a note or doc-source are neutralized (CMH-COPY-08/09)`; `tests/05-copy-handled.spec.js` - `the untrusted-note fence tilde run outgrows a long tilde run in the note (CMH-COPY-08)`; `tests/12-features.spec.js` - note fencing assertions |
| CMH-COPY-09 | All machine-readable JSON (`HANDLED_IDS_JSON`, `NOTES_STATE_JSON`, `CHECKLIST_STATE_JSON`) is emitted only inside ONE unconditional final `=== CMH MACHINE TRAILER (do not edit) ===` block (canonical empty values when there are no changes); the three Python apply tools read JSON only from within that fenced trailer (last open marker) and FAIL CLOSED when the opening marker has no matching END marker, so a forged trailer/state line in a note body or doc-source is ignored. | `tests/05-copy-handled.spec.js` - `forged trailer/instruction lines in a note or doc-source are neutralized (CMH-COPY-08/09)`; `tests/test_notes_apply.py` - forged early NOTES_STATE line ignored, unclosed trailer fails closed; `tests/test_checklist_apply.py` - forged early CHECKLIST_STATE line ignored, unclosed trailer fails closed; `tests/test_mark_handled.py` - forged early HANDLED line ignored, unclosed trailer fails closed |
| CMH-HANDLED-01 | The agent treats the `HANDLED_IDS_JSON` line inside the final `=== CMH MACHINE TRAILER (do not edit) ===` block as the machine-readable contract and appends those ids to `<script id="handledCommentIds">`; a forged `HANDLED_IDS_JSON` line elsewhere in the bundle is ignored. | `tests/test_mark_handled.py` - `test_bundle_parsing`, `test_bundle_parsing_uses_last_authoritative_contract`, `test_cli_from_bundle_stdin` |
| CMH-HANDLED-02 | On load, ids in `handledCommentIds` are pruned from memory and `localStorage`, and matching highlights or rings disappear. | `tests/05-copy-handled.spec.js` - `marking an id handled prunes it on reload and from Copy all (via mark_handled.py)`; `tests/04-comments-code-mermaid.spec.js` - mermaid prune test |
| CMH-HANDLED-03 | Copy all filters handled comments even if storage still contains them, so handled ids never reappear in the bundle or count. | `tests/05-copy-handled.spec.js` - handled prune and copy test; `tests/12-features.spec.js` - handled mermaid copy exclusion |
| CMH-HANDLED-04 | `mark_handled.py` edits only the handled-id JSON block, dedupes ids, validates id shape, preserves order and newline style, and refuses ambiguous duplicate blocks. | `tests/test_mark_handled.py` - only-block change, dedupe/order, unsafe id, CRLF, duplicate blocks, CLI tests |

## Export, embedded comments, and output modes

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-EXP-01 | `Export as Portable` downloads `<stem>-portable.html`, strips any existing `-comments` or `-portable` suffix before adding it, and writes current comments into `<script id="embeddedComments">` with `<` escaped as `\u003c`. | `tests/06-save-export.spec.js` - closing-script escaping and embeds comment; `tests/28-duck-fixes.spec.js` - `Export as Portable does not stack suffixes on a *-comments.html file`; source: `assets/js/` - `_suggestedFilename()` |
| CMH-EXP-02 | A downloaded copy with embedded comments opens in a fresh browser and shows those comments without relying on existing `localStorage`. | `tests/06-save-export.spec.js` - `embedded comments travel: a shared copy shows them in a fresh browser (no localStorage)`; `tests/34-modes.spec.js` - `MODE Portable-with-comments: Save embeds comments that travel to a fresh browser and stay Portable` |
| CMH-EXP-03 | On load, embedded comments merge into localStorage by id; the item with the later `updatedAt`, or fallback `createdAt`, wins, and disjoint ids are preserved. | `tests/10-more.spec.js` - `embedded comments merge by latest updatedAt (both directions)`; source: `assets/js/` - `mergeCommentSets()`, `loadComments()` |
| CMH-EXP-04 | `Export to Plain HTML` removes handled ids, embedded comments, the real `commentableHtmlLayer` descriptor, comment UI, and runtime JS while preserving host content, shipped content styling, localStorage, and host scripts that only carry a decoy `data-id="commentableHtmlLayer"` attribute. | `tests/06-save-export.spec.js` - plain strips layer but keeps content; `tests/12-features.spec.js` - unwraps highlights, drops `sidebar-open`, leaves localStorage; `tests/10-more.spec.js` - forged marker no leak, `Export plain ignores a data-id commentableHtmlLayer decoy script` |
| CMH-EXP-05 | In nonportable mode, Plain HTML keeps the CSS companion link for styling and removes only the runtime companion and comment DOM. | `tests/10-more.spec.js` - `Export plain in nonportable mode strips the JS companion but keeps the CSS companion and content` |
| CMH-EXP-06 | On `file://` (where `fetch()` of the page is blocked) every export falls back to a DOM snapshot as its base. The snapshot is captured before any runtime mutation but is completed at export time with the host content parsed after the layer `<script>` (per charts-embedding.md, chart data and init scripts placed after the `END: commentable-html - JS` marker), so a chart or other tail content is never dropped from a file:// Portable or Plain export. The recovered tail excludes layer-injected chrome (footer, side-TOC, scroll progress, tooltips - all `cm-skip`) so that chrome never leaks into an export. | `tests/45-file-export-tail.spec.js` - Portable and Plain each keep a chart placed after the JS marker; `tests/20-ui-chrome.spec.js`, `tests/22-footer-show.spec.js` - the side-TOC/Help/footer never leak into a Plain or embedded-comment export; source: `assets/js/` - `_snapshotWithTail()`, `CMH_LAYER_SCRIPT` |
| CMH-EXP-07 | The embedded-comments block is located by a real, whitespace-delimited `id="embeddedComments"` attribute regardless of the remaining attribute order or spacing, so a document authored or re-saved as `<script id="embeddedComments" type="application/json">` still receives the current comments on export. A decoy `data-id="embeddedComments"` or `aria-id="embeddedComments"` on another script is never mistaken for the real block, so the export cannot update the wrong script and leave real comments stale. | `tests/47-hardening.spec.js` - `embedded-comments script is found regardless of attribute order (CMH-EXP-07)`, `export updates the real embeddedComments block, not a decoy data-id script (CMH-EXP-07)`; source: `assets/js/` - `_buildSavedHtml()` |
| CMH-EXP-08 | Save, Portable, and Offline exports filter the canonical export comment set through `handledCommentIds`, matching Copy all: a comment already marked handled never reappears in embedded export JSON even if it is still present in live memory. | `tests/06-save-export.spec.js` - `Save, Portable, and Offline exports exclude comments already listed as handled`; source: `assets/js/` - `_exportableComments()` |
| CMH-EXP-09 | Every export path (Save, Portable, Offline, and Plain) normalizes the base HTML by stripping the transient runtime body-state classes (`sidebar-open`, `cm-sidebar-resizing`, `cm-widget-dragging`) from the `<body>` open tag, so a stale or open-sidebar source never bakes that state into a saved or exported file (a persisted `sidebar-open` would render the content full width with an empty sidebar gutter via the `body.sidebar-open .app` layout rule). The normalizer touches only the FIRST `<body>` open tag (a `<body class=...>` literal inside a later script/content is left alone), handles double-quoted, single-quoted, and unquoted class values, matches whole class tokens (a superstring like `x-sidebar-open` is preserved), and removes an emptied class attribute. Non-transient body classes are preserved; the live layer re-derives the sidebar state on load. | `tests/54-export-body-normalize.spec.js` - `Save/Portable export drops sidebar-open and other transient body classes (CMH-EXP-09)`, `Offline export drops sidebar-open and other transient body classes (CMH-EXP-09)`, `Plain export drops sidebar-open and other transient body classes (CMH-EXP-09)`, `Portable export from a nonportable file (snapshot fallback) drops transient body classes (CMH-EXP-09)`, and the `Body-class normalizer unit (CMH-EXP-09)` suite (`single-quoted transient class is stripped to the kept classes`, `a <body class> literal after the real body (in a script) is not mutated`, `a superstring host class like x-sidebar-open is preserved`, `the existing double-quoted case still strips only the transient token`, `an emptied class attribute is removed and unquoted values are handled`); source: `assets/js/` - `_stripTransientBodyClasses()`, `_getBaseHtml()`, `_buildPlainHtml()` |
| CMH-EXP-10 | `Export as Portable` preserves a comment note verbatim even when it contains a `$`-replacement pattern (`$&`, `$1`, `$$`, and the dollar-backtick and dollar-quote forms): the saved-HTML builder splices the embedded-comments region with a function replacer, so `String.replace` never expands those patterns from the note text and the stored JSON round-trips exactly on reload. | `tests/55-export-dollar-and-markers.spec.js` - `Export as Portable preserves a comment note containing $-replacement patterns (F2)`; source: `assets/js/` - `_buildSavedHtml()` |
| CMH-EXP-11 | `Export to Plain HTML` strips a comment-data region whose banner uses the compact single-line marker form (no `===` decoration) that the validator's `=*` marker grammar also accepts, so a compact-markered document exports to plain cleanly without tripping the leak-guard safety net; the leak guard itself matches `id="handledCommentIds"`/`id="embeddedComments"` regardless of quote style or surrounding whitespace. | `tests/55-export-dollar-and-markers.spec.js` - `Export as Plain strips a compact-markered EMBEDDED COMMENTS region (F3)`; source: `assets/js/` - `_buildPlainHtml()` |
| CMH-EXP-12 | The export path (a proven bug hot spot) is guarded by a property/fuzz test and a cross-implementation grammar test: a batch of adversarial comment notes (every `$`-replacement pattern, escaped `<`, `<!--`, `</script`, unicode, backticks, and literal region-marker phrases) each round-trips byte-exact through Export as Portable and `JSON.parse` with no expansion, escaping loss, truncation, or forged region boundary; and Export to Plain strips compact-markered comment-data regions across all of HANDLED IDS, EMBEDDED COMMENTS, and COMMENT UI (not just one), matching the validator's `=*` grammar. | `tests/56-export-fuzz.spec.js` - `Export as Portable round-trips a batch of adversarial comment notes byte-exact (GH-REGRESS-EXPORT-HOTSPOT)`, `Export as Plain strips compact-markered comment-data regions across all of them (GH-REGRESS-EXPORT-HOTSPOT)`; source: `assets/js/` - `_buildSavedHtml()`, `_buildPlainHtml()` |
| CMH-EXP-13 | The comments sidebar exposes a single `Export` disclosure instead of separate always-visible Portable, Offline, Markdown, and Plain HTML buttons. Opening the disclosure toggles `aria-expanded` and reveals ordinary buttons (no ARIA menu/menuitem contract) with the same ids and handlers; Escape closes only that disclosure before any composer draft behind it. | `tests/06-save-export.spec.js` - `sidebar export actions live in a single disclosure and Portable still downloads (CMH-EXP-13)`, `Escape closes only the sidebar export disclosure before a composer draft (CMH-EXP-13)`; source: `assets/template.shell.html`, `assets/js/55-toolbar-menu.js`, `assets/css/20-chrome.css` |
| CMH-MODE-01 | An inline document with embedded assets and either no live comments or all current comments embedded reports `Portable` from `currentDocState()`. | source: `assets/js/` - `currentDocState()`, `updateDocTypeUi()`; `tests/01-inline.spec.js` - initializes as a portable file; `tests/34-modes.spec.js` - `MODE Portable: a fresh self-contained document reports Portable` |
| CMH-MODE-02 | NonPortable mode loads CSS, runtime JS, and an assets registry from companion files, keeps state blocks inline, and marks the page `cm-nonportable`. | `tests/07-nonportable.spec.js` - `loads from companion files and reports itself as needing them`; `tests/test_validate_nonportable.py` - nonportable clean tests |
| CMH-MODE-03 | In nonportable mode, `Export as Portable` rebuilds one self-contained HTML with comments embedded and companion CSS/JS inlined from the loaded assets registry, and downloads it under the same `<stem>-portable.html` name that inline export uses (not a `.standalone.html` name). | `tests/07-nonportable.spec.js` - nonportable export standalone; `tests/12-features.spec.js` - `Export with embedded comments makes a portable standalone file from an nonportable doc` asserts the `-portable.html` suffix; source: `assets/js/` - `saveStandalone()`, `_inlineNonPortableAssets()`, `_suggestedFilename()` |
| CMH-MODE-04 | If nonportable companion files do not initialize within the watchdog window, the visible missing-asset banner appears and runtime-ready is never set. | `tests/07-nonportable.spec.js` - missing companions banner; `tests/test_validate_nonportable.py` - missing banner and watchdog tests |
| CMH-MODE-05 | The nonportable version handshake compares the document meta version, assets registry version, and runtime `CMH_VERSION` with semver compatibility: a same-major runtime that is newer than or equal to the page is compatible and shows no banner, a same-major runtime older than the page shows a soft update banner, and a different major shows an incompatible-runtime banner. Malformed versions fall back to raw-string mismatch warnings without crashing. | `tests/07-nonportable.spec.js` - older same-major no banner, different-major incompatible banner, newer same-major soft banner; source: `assets/js/` - `parseSemver()`, `runtimeCompatibleWith()`, `versionBannerMessage()` |
| CMH-MODE-06 | The document-type bubble is three-state: `Portable`, `Offline`, or `Not portable`. `Not portable` lists three reasons: nonportable references external skill/companion resources, live comments are not embedded, or embedded comments deleted this session still sit in the file until re-export. Embedded-comment deletions persist across reload via tombstones. The bubble is color-coded (Portable green, Not portable orange), and the badge exposes the current reason through `aria-label` plus a polite live region. | `tests/27-ui-batch3.spec.js` - `a Portable file becomes Not portable after deleting an embedded comment`; `tests/28-duck-fixes.spec.js` - `deleting an embedded comment keeps it deleted across reload`; `tests/37-ui-polish.spec.js` - `a Portable doc-type badge is green (CMH-BADGE-COLOR-01)`, `a Not-portable badge is orange and its tooltip says how to share (CMH-BADGE-COLOR-01)`; `tests/34-modes.spec.js` - `MODE Not portable: a live comment that is not embedded flips the badge to Not portable`; `tests/49-offline-export.spec.js` - Offline badge aria-live and aria-label assertions; source: `assets/js/` - `currentDocState()`, `_tombstoneEmbedded()` |
| CMH-MODE-07 | `Export as Portable` in nonportable mode aborts with a clear message when the loaded companion assets registry (`__COMMENTABLE_ASSETS__.version`) differs from the running layer's `CMH_VERSION`, rather than inlining a mismatched runtime into the exported file; no file is downloaded on abort. | `tests/47-hardening.spec.js` - `standalone export aborts when the companion assets version mismatches the runtime (CMH-MODE-07)`; source: `assets/js/` - `_inlineNonPortableAssets()` |
| CMH-MODE-08 | The Output modes reference (`references/exports.md`) documents what is bundled in the file versus fetched from where with two mermaid diagrams (Portable inlines the layer CSS/JS; NonPortable loads `commentable-html.{css,js,assets.js}` from the skill `dist/`; both keep content and comments inline and fetch optional mermaid/Chart.js from a CDN), and `SKILL.md`'s Output modes section links to them. | `tests/test_docs_diagrams.py` - `PortabilityDiagramsTests`; source: `references/exports.md`, `SKILL.md` |
| CMH-DOC-01 | The shipped plugin `README.md` carries a "Why not just plan in chat, Markdown, or plain HTML?" medium-comparison table (chat / Markdown / plain HTML / commentable-html) and both the README and `SKILL.md` cite the "unreasonable effectiveness of HTML" blog post. | `tests/test_docs_diagrams.py` - `MotivationDocsTests`; source: `README.md`, `SKILL.md` |
| CMH-DOC-02 | The shipped `SKILL.md` and tutorial document the `data-cm-draggable` widget drag opt-in, the doc-type badge states (the badge reads `Portable`, `Offline`, or `Not portable`), Export Offline, and the intended mode choices: NonPortable for fast iteration, Portable for peer review, and Offline for zero-network handoff after mermaid and charts render. | `tests/test_docs_diagrams.py` - `NewFeatureDocsTests` (including `test_tutorial_documents_offline_export`, which asserts the exact `Portable`/`Offline`/`Not portable` badge-state wording); source: `SKILL.md`, `docs/TUTORIAL.md` |
| CMH-DOC-03 | The shipped `SKILL.md` stays token-efficient by keeping generation-critical routing, caveats, commands, and compact authoring guardrails inline while moving runtime UI, sort order, embedded-delete durability, interaction details, NonPortable guardrails, network caveats, and manual retrofit fallback into existing references that the skill links to. The compact authoring guardrails include private class prefixes for custom components and reserving `cmh-*` class names for the framework. | `tests/test_docs_diagrams.py` - `SkillTrimDocsTests`; source: `SKILL.md`, `references/document-layout.md`, `references/interaction-model.md`, `references/exports.md`, `references/retrofitting.md` |
| CMH-DOC-04 | Every shipped `references/*.md` page is reachable from either `SKILL.md` or `references/file-inventory.md`, so reference files cannot become orphaned when docs are trimmed or reorganized. | `tests/test_docs_diagrams.py` - `ReferenceReachabilityDocsTests`; source: `SKILL.md`, `references/file-inventory.md` |
| CMH-DOC-05 | Every shipped `references/*.md` page longer than 100 lines opens with a `## Contents` table of contents (as its first level-2 section) whose anchor links cover all of that file's own level 2-3 section headings and each link resolves to a real heading (GitHub-compatible slugs, no duplicate slugs), so a partial read still sees the file's full scope. | `tests/test_docs_diagrams.py` - `ReferenceTocDocsTests`; source: `references/charts-embedding.md`, `references/charts-recipes.md`, `references/exports.md`, `references/document-layout.md`, `references/comment-data-shape.md`, `references/retrofitting.md` |
| CMH-DOC-06 | The shipped `SKILL.md` YAML front-matter `description` is the discovery string the agent reads, so it is a plain single-line scalar that states both what the skill does and when to use it (an explicit `Use when ...` trigger clause naming the HTML review/comment capability), carries the `cmh` shorthand so the skill auto-triggers on it, stays under the marketplace validator's 800-char cap, uses no XML tags, and avoids the reserved `anthropic`/`claude` words. | `tests/test_docs_diagrams.py` - `FrontmatterDescriptionDocsTests`; source: `SKILL.md`, `scripts/validate_marketplace.py` |
| CMH-DOC-07 | Every shipped `references/*.md` page is linked directly from `SKILL.md` as a real Markdown link (one level deep), so progressive disclosure never hides a reference behind another reference file or a bare prose mention. | `tests/test_docs_diagrams.py` - `DirectReferenceLinkDocsTests`; source: `SKILL.md` |
| CMH-DOC-08 | The `SKILL.md` discovery description and the `plugin.json` marketplace description are intentionally different surfaces (agent trigger vs human blurb) but share a whole-word core vocabulary (`html`, `comment`, a review-family word, and `portable`), so the two cannot silently drift into telling different stories. | `tests/test_docs_diagrams.py` - `DescriptionConsistencyDocsTests`; source: `SKILL.md`, `plugin.json` |
| CMH-DOC-09 | The `cmh` shorthand is a declared keyword in both `plugin.json` and the commentable-html marketplace entry, so marketplace search and the site keyword chips match the shorthand as well as the skill auto-trigger. | `tests/test_docs_diagrams.py` - `CmhKeywordDocsTests`; source: `plugin.json`, `.github/plugin/marketplace.json` |
| CMH-DOC-10 | The shipped `SKILL.md` and `dist/README.md` show the current version on a clear, human-readable `**Version:** ` + code-span line, so a reader who opens the skill or its `dist/` folder can see which version they have without decoding `manifest.json`. The line is single-sourced from `dev/VERSION`: `build.py` re-stamps it and `--check` fails when either file is stale. | `tests/test_docs_diagrams.py` - `VisibleVersionDocsTests`; `tests/test_build.py` - `test_source_stamps_include_visible_version_in_skill_and_dist_readme`, `StampHelperTests.test_stamp_md_version_updates_the_single_marker`, `test_stamp_md_version_requires_exactly_one_marker`; source: `SKILL.md`, `dist/README.md`, `dev/tools/build.py` |
| CMH-DOC-11 | The shipped plugin `README.md` pitch surfaces two value props of existing behavior: that comments persist in the browser's `localStorage` and survive a browser restart or reboot while iterating, and that `Copy all` returns every comment at once so the agent makes one coordinated, coherent edit across all notes instead of a fragile one-at-a-time pass. | `tests/test_docs_diagrams.py` - `PitchPersistenceAndBatchDocsTests`; source: `README.md` |
| CMH-DOC-12 | The shipped `SKILL.md` states the plugin installs into both Claude Code and the GitHub Copilot CLI (naming the `claude plugin install` / `copilot plugin install commentable-html@urikan-ai-marketplace` commands) and is invokable from each agent's CLI and Desktop app, so the dual-agent support is visible in the discovery doc; the reserved `claude`/`anthropic` words stay out of the front-matter description (CMH-DOC-06). | `tests/test_docs_diagrams.py` - `DualAgentDocsTests.test_skill_states_dual_agent_install_and_cli_and_desktop`, `DualAgentDocsTests.test_skill_frontmatter_description_avoids_reserved_words`; source: `SKILL.md` |
| CMH-DOC-13 | The shipped plugin `README.md` states the plugin installs into both Claude Code and the GitHub Copilot CLI and shows each agent's marketplace-add and install commands (`claude plugin install` / `copilot plugin install commentable-html@urikan-ai-marketplace`), so the shipped README presents the dual-agent story like the `SKILL.md`, the marketplace manifests, and the site. | `tests/test_docs_diagrams.py` - `PluginReadmeDualAgentTests.test_plugin_readme_states_dual_agent_install`; source: `README.md` |
| CMH-DOC-14 | The shipped skill carries the MIT `LICENSE` at its root (matching the repository root `LICENSE`), so every distributed copy - the Copilot/Claude `plugin install` source subtree and the Claude Desktop skill ZIP - includes the required license notice. | `tests/test_docs_diagrams.py` - `ShippedLicenseTests.test_skill_ships_the_mit_license`, `ShippedLicenseTests.test_shipped_license_matches_the_repo_license`; source: `skills/commentable-html/LICENSE` |
| CMH-DOC-15 | Every `tools/...` path mentioned in the shipped plugin `README.md` resolves to a real shipped file under `skills/commentable-html/tools/`, so tool-bucket refactors cannot leave stale authoring or validation paths in the README. | `tests/test_docs_diagrams.py` - `PluginReadmeToolPathDocsTests.test_plugin_readme_tool_paths_resolve`; source: `README.md`, `skills/commentable-html/tools/` |
| CMH-DOC-16 | The shipped Chart.js reference is split so chart embedding/tooltips and chart recipes/data hygiene/dark theme live in separate focused files, while `references/charts.md` remains only a thin compatibility router. `SKILL.md` links directly to all three files so agents can load the narrow guide they need instead of the old combined reference. | `tests/test_docs_diagrams.py` - `ChartReferenceSplitDocsTests`; source: `SKILL.md`, `references/charts.md`, `references/charts-embedding.md`, `references/charts-recipes.md` |
| CMH-MODE-09 | The NonPortable asset/version banner has an accessible `Dismiss` button. Dismissal hides the banner and persists in `localStorage` by document key plus page/runtime version pair, so reloads keep the same warning hidden while a different version pair can still warn. | `tests/07-nonportable.spec.js` - dismissible version banner reload test; source: `assets/js/` - `revealAssetBanner()`, `assetBannerDismissKey()` |

## Export security invariants

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-EXP-14 | The whole-document `DOMParser -> doc.documentElement.outerHTML` round-trip used in the four export/apply paths (`36-checklist.js`, `37-notes.js`, `65-export-portable.js`, `68-export-offline.js`) is safe only because comment/state data never enters it as markup and host HTML is a trusted, unsanitized boundary (CMH-SEC-01). Every `parseFromString()` call site feeds only the trusted document `html` string, never a comment `note`, `quote`, `author`, or any other user-supplied field. If host-HTML sanitization is ever added upstream, this round-trip must be re-audited for serialization-differential stored XSS before landing. | `tests/test_export_domparser_guard.py` - `test_domparser_call_sites_never_take_comment_fields`, `test_domparser_call_sites_use_the_trusted_html_variable`, `test_domparser_present_only_in_expected_partials`; source: `assets/js/36-checklist.js` - `_applyChecklistStateToHtml()`, `assets/js/37-notes.js` - `_applyNoteStateToHtml()`, `assets/js/65-export-portable.js` - `_applyWidgetLayoutToHtml()`, `assets/js/68-export-offline.js` - `_offlineDocFromHtml()` |

## Export to Markdown

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-MD-01 | `Export to Markdown` deterministically converts `#commentRoot` content to GFM (headings, tables, fenced code with language, mermaid source not SVG, lists, blockquotes, image figures, KQL kusto fences with the run link), appends live comments as a `Review comments` section, and downloads a `.md` file (download only, never writing the clipboard), and is reachable from the sidebar and the overflow menu. | `tests/43-export-markdown.spec.js` - kitchen-sink structure, determinism, callout/chart/diff forms, mermaid source, comment appendix, `Export to Markdown downloads a .md file and does NOT write the clipboard`, sidebar/overflow availability; source: `assets/js/` - `htmlToMarkdown()`, `buildMarkdownDoc()`, `exportMarkdown()` |
| CMH-MD-02 | Markdown export is injection-safe: prose, comment notes, and attribute-derived labels cannot forge headings, blockquotes, lists, thematic rules, setext underlines, GFM tables, or raw HTML; link/image labels and URLs are escaped; `javascript:`/control-char URLs are neutralized; code spans and fences stay intact against backtick and pipe tricks. | `tests/43-export-markdown.spec.js` - the forge/escape/neutralize suite (setext, table, backslash destination, control-char scheme, pipe-in-cell, fence voiding, HTML injection); source: `assets/js/` - `_mdText()`, `_mdEscapeLeading()`, `_mdEscapePipes()`, `_mdUrl()` |
| CMH-MD-03 | List items serialize inline `<strong>`/`<em>`/`<a>`/`<code>`/`<img>` that are DIRECT children of the `<li>` (not only inside a nested block), so a bullet mixing `**bold**`, an inline `<a>` link, and `code` keeps its emphasis, link, and code span in the exported Markdown. | `tests/47-hardening.spec.js` - `Markdown export serializes strong/link/code that are direct list-item children (CMH-MD-03)`; source: `assets/js/` - `_mdInlineOne()`, `_mdAppendInline()`, `_mdList()` |
| CMH-MD-04 | The Markdown URL allowlist keeps only image data URLs: a bare `data:` destination (for example `data:text/html`) is neutralized to `about:blank`, while a `data:image/...` source is preserved so inline images still export. | `tests/47-hardening.spec.js` - `Markdown export drops a bare data: URL but keeps a data:image URL (CMH-MD-04)`; source: `assets/js/` - `_mdUrl()` |
| CMH-MD-05 | Markdown export serializes `data-cm-widget` content even when the widget is `cm-skip`: slotted boards export as a `_[Widget: ...]_` note plus a GFM table whose columns are `data-cm-slot` values and whose cells are `data-cm-part-label` values; unslotted widgets export as a bullet list. | `tests/43-export-markdown.spec.js` - `expanded demo item kinds convert to Markdown`; source: `assets/js/` - `_mdWidget()` |
| CMH-MD-06 | Markdown export deliberately omits `data-cm-offline-chart` image payloads to avoid base64 bloat, emitting only the offline chart snapshot's caption or alt text as a chart-snapshot placeholder. | `tests/43-export-markdown.spec.js` - `offline chart snapshots export label text without embedding the data image (CMH-MD-06)`; source: `assets/js/` - `_mdFigure()` |
