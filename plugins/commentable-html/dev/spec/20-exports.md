## Copy-all bundle and handled-id contracts

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-COPY-01 | Copy all emits a Markdown bundle with document label, `Source`, ordered comments, agent instructions, and a final `HANDLED_IDS_JSON` array. | `tests/05-copy-handled.spec.js` - `Copy all emits the markdown bundle with an exact HANDLED_IDS_JSON line`; `tests/12-features.spec.js` - `the bundle carries the full documented contract for a text comment` |
| CMH-COPY-02 | Text-comment bundle entries include `Where`, `Pinpoint`, offsets, quoted text, optional context, containing block, and the user's note. | `tests/12-features.spec.js` - `the bundle carries the full documented contract for a text comment`, `a comment under headings surfaces Where / Pinpoint / In context metadata` |
| CMH-COPY-03 | Copy all uses the same bundle from toolbar and sidebar, and it has anchor-specific payloads for code, mermaid, diff, and image comments. | `tests/12-features.spec.js` - `the top toolbar Copy all copies the same bundle (positive path)`, `a code comment emits a fenced block and omits the prose-only context fields`, `a mermaid comment produces a mermaid Copy-all payload and its ring activates the card`; `tests/13-diff.spec.js` - `Copy all for a region comment quotes only the selected substring, not the whole line`, `Copy all emits the diff anchor and the quoted diff line`, `Copy all for a diff comment includes the HANDLED_IDS_JSON contract and 'added line N'`; `tests/14-image.spec.js` - `Copy all emits an image anchor with the alt text` |
| CMH-COPY-04 | Copy all with no copyable comments or tracked state changes is disabled, copies nothing, and exposes a `No comments to copy` tooltip instead of running the copy path. | `tests/12-features.spec.js` - `Copy all is disabled with zero comments and re-enabled after a comment exists (CMH-COPY-04, CMH-COPY-07)` |
| CMH-COPY-05 | Copy all clipboard writes use `navigator.clipboard` first, then `execCommand`, then a `prompt` fallback. When the user cancels the prompt the toast must not claim success - it must describe what actually happened. | `tests/12-features.spec.js` - `Copy all falls back to execCommand when the async clipboard is unavailable`, `Copy all falls back to prompt() when both clipboard paths fail`; `tests/36-a11y.spec.js` - `Copy all does NOT claim success when it falls back to a manual prompt` |
| CMH-COPY-06 | Generic per-block clipboard actions (code-block Copy buttons and `data-cmh-copy` chips) use `navigator.clipboard` then `execCommand`; if both fail they show a `Copy failed` toast and do NOT open a `prompt` (only Copy all has the prompt fallback). | `tests/38-spec-gaps.spec.js` - `per-block copy failures show Copy failed without opening prompt (CMH-COPY-06)`; source: `assets/js/` - code-block copy and `data-cmh-copy` handlers |
| CMH-COPY-07 | Copy all controls expose `aria-disabled="true"` and the tooltip `No comments to copy` when there is no copyable review state, stay keyboard-focusable for the same tooltip, re-enable once a comment exists, and return to disabled after Clear removes the last comment. | `tests/12-features.spec.js` - `Copy all is disabled with zero comments and re-enabled after a comment exists (CMH-COPY-04, CMH-COPY-07)`; source: `assets/js/` - `updateCopyAllState()` |
| CMH-COPY-08 | Copy all treats reviewer notes, document text, the document label, and the document source as untrusted: it strips Unicode bidi/format controls (`U+202A-U+202E`, `U+2066-U+2069`, `U+200E/U+200F`) from human-readable payload text, and escapes those controls in machine-readable JSON identifiers so apply tools still parse the original ids without raw invisible controls in the bundle. Each free-text note is otherwise wrapped in a dynamic, tilde-sized `UNTRUSTED REVIEWER NOTE` fence (longer than any tilde run in the note), a read-first AGENT INSTRUCTIONS block frames notes as document-scoped change requests (data, not agent/system instructions), and `data-doc-source` is emitted single-line with backticks neutralized so an embedded newline cannot forge a standalone machine line and a backtick cannot close the Markdown code span it sits in. | `tests/05-copy-handled.spec.js` - `forged trailer/instruction lines in a note or doc-source are neutralized (CMH-COPY-08/09)`; `tests/05-copy-handled.spec.js` - `bidi formatting controls in notes, document text, and metadata are stripped from Copy all (CMH-COPY-08)`; `tests/05-copy-handled.spec.js` - `the untrusted-note fence tilde run outgrows a long tilde run in the note (CMH-COPY-08)`; `tests/12-features.spec.js` - `the bundle carries the full documented contract for a text comment` |
| CMH-COPY-09 | All machine-readable JSON (`HANDLED_IDS_JSON`, `NOTES_STATE_JSON`, `CHECKLIST_STATE_JSON`) is emitted only inside ONE unconditional final `=== CMH MACHINE TRAILER (do not edit) ===` block (canonical empty values when there are no changes); the three Python apply tools read JSON only from within that fenced trailer (last open marker) and FAIL CLOSED when the opening marker has no matching END marker, so a forged trailer/state line in a note body or doc-source is ignored. | `tests/05-copy-handled.spec.js` - `forged trailer/instruction lines in a note or doc-source are neutralized (CMH-COPY-08/09)`; `tests/test_notes_apply.py` - `test_from_bundle_reads_only_the_machine_trailer`, `test_from_bundle_ignores_forged_line_when_no_changes`, `test_from_bundle_fails_closed_on_unclosed_trailer`; `tests/test_checklist_apply.py` - `test_from_bundle_ignores_forged_line_outside_trailer`, `test_from_bundle_ignores_forged_line_when_no_changes`, `test_from_bundle_fails_closed_on_unclosed_trailer`; `tests/test_mark_handled.py` - `test_bundle_parsing_uses_last_authoritative_contract`, `test_bundle_parsing_fails_closed_on_unclosed_trailer` |
| CMH-HANDLED-01 | The agent treats the `HANDLED_IDS_JSON` line inside the final `=== CMH MACHINE TRAILER (do not edit) ===` block as the machine-readable contract and appends those ids to `<script id="handledCommentIds">`; a forged `HANDLED_IDS_JSON` line elsewhere in the bundle is ignored. | `tests/test_mark_handled.py` - `test_bundle_parsing`, `test_bundle_parsing_uses_last_authoritative_contract`, `test_cli_from_bundle_stdin` |
| CMH-HANDLED-02 | On load, ids in `handledCommentIds` are pruned from memory and `localStorage`, and matching highlights or rings disappear. | `tests/05-copy-handled.spec.js` - `marking an id handled prunes it on reload and from Copy all (via mark_handled.py)`; `tests/04-comments-code-mermaid.spec.js` - `a mermaid comment prunes on reload once marked handled` |
| CMH-HANDLED-03 | Copy all filters handled comments even if storage still contains them, so handled ids never reappear in the bundle or count. | `tests/05-copy-handled.spec.js` - `marking an id handled prunes it on reload and from Copy all (via mark_handled.py)`; `tests/12-features.spec.js` - `a handled mermaid comment is excluded from Copy all after reload` |
| CMH-HANDLED-04 | `mark_handled.py` edits only the handled-id JSON block, dedupes ids, validates id shape, preserves order and newline style, and refuses ambiguous duplicate blocks. | `tests/test_mark_handled.py` - `test_only_the_block_changes`, `test_dedupes_and_preserves_order`, `test_rejects_unsafe_id_without_writing`, `test_preserves_crlf`, `test_rejects_multiple_real_blocks`, `test_cli_appends_and_is_idempotent` |

## Export, embedded comments, and output modes

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-EXP-01 | `Export as Shareable` downloads `<stem>-shareable.html`, strips any existing `-comments`, `-shareable`, or pre-rename `-portable` suffix before adding it (so re-exporting a file an earlier release produced yields `<stem>-shareable.html`, never `<stem>-portable-shareable.html`), and writes current comments into `<script id="embeddedComments">` with `<` escaped as `\u003c`. `Export Offline` strips the same set. | `tests/06-save-export.spec.js` - `a comment note with a closing-script tag is escaped and round-trips decoded`, `Save comments embeds the comment into the downloaded copy`; `tests/28-duck-fixes.spec.js` - `Export as Shareable does not stack suffixes on a *-comments.html file`; `tests/test_shareable_compat.py` - `LegacyExportFilenameTests.test_shareable_and_offline_filenames_strip_the_legacy_suffix`; `tests/51-legacy-compat.spec.js` - `re-exporting a file named by an earlier release does not stack the legacy suffix (CMH-EXP-01)`; source: `assets/js/` - `_suggestedFilename()` |
| CMH-EXP-02 | A downloaded copy with embedded comments opens in a fresh browser and shows those comments without relying on existing `localStorage`. | `tests/06-save-export.spec.js` - `embedded comments travel: a shared copy shows them in a fresh browser (no localStorage)`; `tests/34-modes.spec.js` - `MODE Shareable-with-comments: Save embeds comments that travel to a fresh browser and stay Shareable` |
| CMH-EXP-03 | On load, embedded comments merge into localStorage by id; the item with the later `updatedAt`, or fallback `createdAt`, wins, and disjoint ids are preserved. | `tests/10-more.spec.js` - `embedded comments merge by latest updatedAt (both directions)`; source: `assets/js/` - `mergeCommentSets()`, `loadComments()` |
| CMH-EXP-04 | `Export to Plain HTML` removes handled ids, embedded comments, the real `commentableHtmlLayer` descriptor, comment UI, and runtime JS while preserving host content, shipped content styling, localStorage, and host scripts that only carry a decoy `data-id="commentableHtmlLayer"` attribute. | `tests/06-save-export.spec.js` - `Export plain strips the layer but keeps the content`; `tests/12-features.spec.js` - `Export plain unwraps highlights, drops sidebar-open, keeps content, and leaves localStorage intact`; `tests/10-more.spec.js` - forged marker no leak, `Export plain ignores a data-id commentableHtmlLayer decoy script` |
| CMH-EXP-05 | In nonshareable mode, Plain HTML keeps the CSS companion link for styling and removes only the runtime companion and comment DOM. | `tests/10-more.spec.js` - `Export plain in nonshareable mode strips the JS companion but keeps the CSS companion and content` |
| CMH-EXP-06 | On `file://` (where `fetch()` of the page is blocked) every export falls back to a DOM snapshot as its base. The snapshot is captured before any runtime mutation but is completed at export time with the host content parsed after the layer `<script>` (per charts-embedding.md, chart data and init scripts placed after the `END: commentable-html - JS` marker), so a chart or other tail content is never dropped from a file:// Shareable or Plain export. The recovered tail excludes layer-injected chrome (footer, side-TOC, scroll progress, tooltips - all `cm-skip`) so that chrome never leaks into an export. | `tests/45-file-export-tail.spec.js` - `Export as Shareable keeps a chart placed after the JS marker`, `Export as Plain keeps a chart placed after the JS marker`; `tests/20-ui-chrome.spec.js` - `the TOC side menu and Help modal never leak into a Plain HTML export`, `the TOC side menu and Help modal never leak into an Export with embedded comments`; `tests/22-footer-show.spec.js` - `the footer does not leak into a Plain HTML export`, `the Report an issue link does not leak into a Plain HTML export (CMH-FOOT-05)`, `the brand site link does not leak into a Plain HTML export`, `the copy-session control does not leak into a Plain HTML export (CMH-FOOT-04)`; source: `assets/js/` - `_snapshotWithTail()`, `CMH_LAYER_SCRIPT` |
| CMH-EXP-07 | The embedded-comments block is located by a real, parsed `id` attribute regardless of the remaining attribute order or spacing, so a document authored or re-saved as `<script id="embeddedComments" type="application/json">` still receives the current comments on export. A decoy `data-id="embeddedComments"` or `aria-id="embeddedComments"` on another script is never mistaken for the real block, so the export cannot update the wrong script and leave real comments stale. | `tests/47-hardening.spec.js` - `embedded-comments script is found regardless of attribute order (CMH-EXP-07)`, `export updates the real embeddedComments block, not a decoy data-id script (CMH-EXP-07)`; source: `assets/js/` - `_buildSavedHtml()` |
| CMH-EXP-08 | Save, Shareable, and Offline exports filter the canonical export comment set through `handledCommentIds`, matching Copy all: a comment already marked handled never reappears in embedded export JSON even if it is still present in live memory. | `tests/06-save-export.spec.js` - `Save, Shareable, and Offline exports exclude comments already listed as handled`; source: `assets/js/` - `_exportableComments()` |
| CMH-EXP-09 | Every export path (Save, Shareable, Offline, and Plain) normalizes the base HTML by stripping the transient runtime body-state classes (`sidebar-open`, `cm-sidebar-resizing`, `cm-widget-dragging`) from the `<body>` open tag, so a stale or open-sidebar source never bakes that state into a saved or exported file (a persisted `sidebar-open` would render the content full width with an empty sidebar gutter via the `body.sidebar-open .app` layout rule). The normalizer touches only the FIRST `<body>` open tag (a `<body class=...>` literal inside a later script/content is left alone), handles double-quoted, single-quoted, and unquoted class values, matches whole class tokens (a superstring like `x-sidebar-open` is preserved), and removes an emptied class attribute. Non-transient body classes are preserved; the live layer re-derives the sidebar state on load. | `tests/54-export-body-normalize.spec.js` - `Save/Shareable export drops sidebar-open and other transient body classes (CMH-EXP-09)`, `Offline export drops sidebar-open and other transient body classes (CMH-EXP-09)`, `Plain export drops sidebar-open and other transient body classes (CMH-EXP-09)`, `Shareable export from a nonshareable file (snapshot fallback) drops transient body classes (CMH-EXP-09)`, and the `Body-class normalizer unit (CMH-EXP-09)` suite (`single-quoted transient class is stripped to the kept classes (CMH-EXP-09)`, `a <body class> literal after the real body (in a script) is not mutated (CMH-EXP-09)`, `a superstring host class like x-sidebar-open is preserved (CMH-EXP-09)`, `the existing double-quoted case still strips only the transient token (CMH-EXP-09)`, `an emptied class attribute is removed and unquoted values are handled (CMH-EXP-09)`); source: `assets/js/` - `_stripTransientBodyClasses()`, `_getBaseHtml()`, `_buildPlainHtml()` |
| CMH-EXP-10 | `Export as Shareable` preserves a comment note verbatim even when it contains a `$`-replacement pattern (`$&`, `$1`, `$$`, and the dollar-backtick and dollar-quote forms): the saved-HTML builder splices the embedded-comments region with a function replacer, so `String.replace` never expands those patterns from the note text and the stored JSON round-trips exactly on reload. | `tests/55-export-dollar-and-markers.spec.js` - `Export as Shareable preserves a comment note containing $-replacement patterns (F2)`; source: `assets/js/` - `_buildSavedHtml()` |
| CMH-EXP-11 | `Export to Plain HTML` strips a comment-data region whose banner uses the compact single-line marker form (no `===` decoration) that the validator's `=*` marker grammar also accepts, so a compact-markered document exports to plain cleanly without tripping the leak-guard safety net; the leak guard itself matches `id="handledCommentIds"`/`id="embeddedComments"` regardless of quote style or surrounding whitespace. | `tests/55-export-dollar-and-markers.spec.js` - `Export as Plain strips a compact-markered EMBEDDED COMMENTS region (F3)`; source: `assets/js/` - `_buildPlainHtml()` |
| CMH-EXP-12 | The export path (a proven bug hot spot) is guarded by a property/fuzz test and a cross-implementation grammar test: a batch of adversarial comment notes (every `$`-replacement pattern, escaped `<`, `<!--`, `</script`, unicode, backticks, and literal region-marker phrases) each round-trips byte-exact through Export as Shareable and `JSON.parse` with no expansion, escaping loss, truncation, or forged region boundary; and Export to Plain strips compact-markered comment-data regions across all of HANDLED IDS, EMBEDDED COMMENTS, and COMMENT UI (not just one), matching the validator's `=*` grammar. | `tests/56-export-fuzz.spec.js` - `Export as Shareable round-trips a batch of adversarial comment notes byte-exact (GH-REGRESS-EXPORT-HOTSPOT)`, `Export as Plain strips compact-markered comment-data regions across all of them (GH-REGRESS-EXPORT-HOTSPOT)`; source: `assets/js/` - `_buildSavedHtml()`, `_buildPlainHtml()` |
| CMH-EXP-13 | The comments sidebar exposes a single `Export` disclosure instead of separate always-visible Shareable, Offline, Markdown, and Plain HTML buttons. The `Export` toggle sits in the prominent primary row beside `Copy all` (the more important action, given the more prominent slot; `Search` moved into the compact ribbon). Opening the disclosure toggles `aria-expanded` and reveals ordinary buttons (no ARIA menu/menuitem contract) with the same ids and handlers, styled as clean content-width menu items (transparent until hovered, not full-ribbon-width bordered buttons); Escape closes only that disclosure before any composer draft behind it. | `tests/06-save-export.spec.js` - `sidebar export actions live in a single disclosure and Shareable still downloads (CMH-EXP-13)`, `Escape closes only the sidebar export disclosure before a composer draft (CMH-EXP-13)`, `the export menu is content-width with clean menu-item buttons (CMH-EXP-13)`; `tests/21-sidebar-polish.spec.js` - `Export sits in the prominent primary row and Search sits in the compact ribbon (CMH-EXP-13)`; source: `assets/template.shell.html`, `assets/js/55-toolbar-menu.js`, `assets/css/20-chrome.css` |
| CMH-EXP-15 | Every export action (Shareable, Offline, Markdown, Plain HTML, and PDF, from either the sidebar Export menu or the collapsed toolbar overflow menu) shows a centered toast naming which export is running, so it is obvious what is being downloaded. The announcement is suppressed exactly when an open comment popover would swallow the click (the export does not run), decided by the popover's own `cmhPopoverWouldSwallowClick` predicate rather than a re-derived copy of it, so it never announces an export that will not happen nor suppresses one that will. Because that predicate swallows only a click that lands in the ANNOTATED DOCUMENT, an export control OUTSIDE the content root - where the layer's own chrome lives in a document that has a `#commentRoot` - is never swallowed: an open dialog closes but the reviewer's FIRST click still runs and announces the export. The suppression branch therefore fires only where an export control IS inside the annotated root: the `#commentRoot`-less `<body>` fallback (CMH-CORE-15), and document content carrying an export control's id. That predicate keys on the dismiss listener being ARMED (registered a tick after the dialog opens), not merely on the dialog existing, so a click in the pre-registration window - which nothing swallows - still announces. | `tests/06-save-export.spec.js` - `each export shows a centered toast naming the export (CMH-EXP-15)`, `every export control (both menus, all five formats) announces its centered toast label (CMH-EXP-15)`, `the export toast is suppressed when an open comment popover swallows the click (CMH-EXP-15)`, `an export control in the layer's own chrome is never swallowed by an open popover (CMH-EXP-15)`, `the export toast announces while the comment popover is mid-edit, which never swallows (CMH-EXP-15)`, `the export toast announces in the window before the popover's dismiss listener is armed (CMH-EXP-15)`; source: `assets/js/90-toast.js`, `assets/css/20-chrome.css` |
| CMH-MODE-01 | An inline document with embedded assets and either no live comments or all current comments embedded reports `Shareable` from `currentDocState()`. | source: `assets/js/` - `currentDocState()`, `updateDocTypeUi()`; `tests/01-inline.spec.js` - `initializes and reports itself as a shareable file`; `tests/34-modes.spec.js` - `MODE Shareable: a fresh self-contained document reports Shareable` |
| CMH-MODE-02 | NonShareable mode loads CSS, runtime JS, and an assets registry from companion files, keeps state blocks inline, and marks the page `cm-nonshareable`. | `tests/07-nonshareable.spec.js` - `loads from companion files and reports itself as needing them`; `tests/test_validate_nonshareable.py` - `test_minimal_nonshareable_is_clean`, `test_real_nonshareable_template_is_clean`, `test_cache_busted_companion_refs_validate_clean` |
| CMH-MODE-03 | In nonshareable mode, `Export as Shareable` rebuilds one self-contained HTML with comments embedded and companion CSS/JS inlined from the loaded assets registry, and downloads it under the same `<stem>-shareable.html` name that inline export uses (not a `.standalone.html` name). | `tests/07-nonshareable.spec.js` - `Export with embedded comments produces one shareable standalone file in nonshareable mode`; `tests/12-features.spec.js` - `Export with embedded comments makes a shareable standalone file from an nonshareable doc and reopens showing them` asserts the `-shareable.html` suffix; source: `assets/js/` - `saveStandalone()`, `_inlineNonShareableAssets()`, `_suggestedFilename()` |
| CMH-MODE-04 | If nonshareable companion files do not initialize within the watchdog window, the visible missing-asset banner appears and runtime-ready is never set. | `tests/07-nonshareable.spec.js` - `missing companions reveal the banner and never mark the runtime ready`; `tests/test_validate_nonshareable.py` - `test_missing_banner_errors`, `test_missing_watchdog_warns` |
| CMH-MODE-05 | The nonshareable version handshake compares the document meta version, assets registry version, and runtime `CMH_VERSION` with semver compatibility: a same-major runtime that is newer than or equal to the page is compatible and shows no banner, a same-major runtime older than the page shows a soft update banner, and a different major shows an incompatible-runtime banner. Malformed versions fall back to raw-string mismatch warnings without crashing. | `tests/07-nonshareable.spec.js` - `an older same-major page version does not show the version banner`, `a different-major page version shows the incompatible banner`, `a newer same-major page version shows the soft version banner`; source: `assets/js/` - `parseSemver()`, `runtimeCompatibleWith()`, `versionBannerMessage()` |
| CMH-MODE-06 | The document-type bubble is three-state: `Shareable`, `Offline`, or `Not shareable`. `Not shareable` lists three reasons: nonshareable references external skill/companion resources, live comments are not embedded, or embedded comments deleted this session still sit in the file until re-export. Embedded-comment deletions persist across reload via tombstones. The bubble is color-coded (Shareable green, Not shareable orange), and the badge exposes the current reason through `aria-label` plus a polite live region and a `title` tooltip that names the remediation (`Export as Shareable` ... `to share it`). | `tests/27-ui-batch3.spec.js` - `a Shareable file becomes Not shareable after deleting an embedded comment`; `tests/28-duck-fixes.spec.js` - `deleting an embedded comment keeps it deleted across reload`; `tests/37-ui-polish.spec.js` - `a Shareable doc-type badge is green (CMH-MODE-06)`, `a Not-shareable badge is orange and its tooltip says how to share (CMH-MODE-06)`; `tests/34-modes.spec.js` - `MODE Not shareable: a live comment that is not embedded flips the badge to Not shareable`; `tests/49-offline-export.spec.js` - `Export Offline embeds vendored mermaid and Chart.js for zero-network reopen (CMH-OFFLINE-01, CMH-OFFLINE-02)`; source: `assets/js/` - `currentDocState()`, `_tombstoneEmbedded()` |
| CMH-MODE-07 | `Export as Shareable` in nonshareable mode aborts with a clear message when the loaded companion assets registry (`__COMMENTABLE_ASSETS__.version`) differs from the running layer's `CMH_VERSION`, rather than inlining a mismatched runtime into the exported file; no file is downloaded on abort. | `tests/47-hardening.spec.js` - `standalone export aborts when the companion assets version mismatches the runtime (CMH-MODE-07)`; source: `assets/js/` - `_inlineNonShareableAssets()` |
| CMH-MODE-08 | The Output modes reference (`references/exports.md`) documents what is bundled in the file versus fetched from where with two mermaid diagrams (Shareable inlines the layer CSS/JS; NonShareable loads `commentable-html.{css,js,assets.js}` from the skill `dist/`; both keep content and comments inline and fetch optional mermaid/Chart.js from a CDN), and `SKILL.md`'s Output modes section links to them. | `tests/test_docs_diagrams.py` - `ShareabilityDiagramsTests`; source: `references/exports.md`, `SKILL.md` |
| CMH-DOC-01 | The shipped plugin `README.md` carries a "Why not just plan in chat, Markdown, or plain HTML?" medium-comparison table (chat / Markdown / plain HTML / commentable-html) and both the README and `SKILL.md` cite the "unreasonable effectiveness of HTML" blog post. | `tests/test_docs_diagrams.py` - `MotivationDocsTests`; source: `README.md`, `SKILL.md` |
| CMH-DOC-02 | The shipped `SKILL.md` and tutorial document the `data-cm-draggable` widget drag opt-in, the doc-type badge states (the badge reads `Shareable`, `Offline`, or `Not shareable`), Export Offline, and the intended mode choices: NonShareable for fast iteration, Shareable for peer review, and Offline for zero-network handoff after mermaid and charts render. | `tests/test_docs_diagrams.py` - `NewFeatureDocsTests` (including `test_tutorial_documents_offline_export`, which asserts the exact Shareable/Offline/Not shareable badge-state wording); source: `SKILL.md`, `docs/TUTORIAL.md` |
| CMH-DOC-03 | The shipped `SKILL.md` stays token-efficient by keeping generation-critical routing, caveats, commands, and compact authoring guardrails inline while moving runtime UI, sort order, embedded-delete durability, interaction details, NonShareable guardrails, network caveats, manual retrofit fallback, and deep deck workflow guidance into references that the skill links to. The compact authoring guardrails include private class prefixes for custom components and reserving `cmh-*` class names for the framework. | `tests/test_docs_diagrams.py` - `SkillTrimDocsTests`; source: `SKILL.md`, `references/document-layout.md`, `references/interaction-model.md`, `references/exports.md`, `references/retrofitting.md`, `references/deck-design.md`, `references/deck-contract.md` |
| CMH-DOC-04 | Every shipped `references/*.md` page is reachable from either `SKILL.md` or `references/file-inventory.md`, so reference files cannot become orphaned when docs are trimmed or reorganized. | `tests/test_docs_diagrams.py` - `ReferenceReachabilityDocsTests`; source: `SKILL.md`, `references/file-inventory.md` |
| CMH-DOC-05 | Every shipped `references/*.md` page longer than 100 lines opens with a `## Contents` table of contents (as its first level-2 section) whose anchor links cover all of that file's own level 2-3 section headings and each link resolves to a real heading (GitHub-compatible slugs, no duplicate slugs), so a partial read still sees the file's full scope. | `tests/test_docs_diagrams.py` - `ReferenceTocDocsTests`; source: `references/charts-embedding.md`, `references/charts-recipes.md`, `references/exports.md`, `references/document-layout.md`, `references/comment-data-shape.md`, `references/retrofitting.md` |
| CMH-DOC-06 | The shipped `SKILL.md` YAML front-matter `description` is the discovery string the agent reads, so it is a plain single-line scalar that states both what the skill does and when to use it (an explicit `Use when ...` trigger clause naming the HTML review/comment capability), carries the `cmh` shorthand so the skill auto-triggers on it, stays under the marketplace validator's 800-char cap, uses no XML tags, and avoids the reserved `anthropic`/`claude` words. | `tests/test_docs_diagrams.py` - `FrontmatterDescriptionDocsTests`; source: `SKILL.md`, `scripts/validate_marketplace.py` |
| CMH-DOC-07 | Every shipped `references/*.md` page is linked directly from `SKILL.md` as a real Markdown link (one level deep), so progressive disclosure never hides a reference behind another reference file or a bare prose mention. | `tests/test_docs_diagrams.py` - `DirectReferenceLinkDocsTests`; source: `SKILL.md` |
| CMH-DOC-08 | The `SKILL.md` discovery description and the `plugin.json` marketplace description are intentionally different surfaces (agent trigger vs human blurb) but share a whole-word core vocabulary (`html`, `comment`, a review-family word, and `shareable`), so the two cannot silently drift into telling different stories. | `tests/test_docs_diagrams.py` - `DescriptionConsistencyDocsTests`; source: `SKILL.md`, `plugin.json` |
| CMH-DOC-09 | The `cmh` shorthand is a declared keyword in both `plugin.json` and the commentable-html marketplace entry, so marketplace search and the site keyword chips match the shorthand as well as the skill auto-trigger. | `tests/test_docs_diagrams.py` - `CmhKeywordDocsTests`; source: `plugin.json`, `.github/plugin/marketplace.json` |
| CMH-DOC-10 | The shipped `SKILL.md` and `dist/README.md` show the current version on a clear, human-readable `**Version:** ` + code-span line, so a reader who opens the skill or its `dist/` folder can see which version they have without decoding `manifest.json`. The line is single-sourced from `dev/VERSION`: `build.py` re-stamps it and `--check` fails when either file is stale. | `tests/test_docs_diagrams.py` - `VisibleVersionDocsTests`; `tests/test_build.py` - `test_source_stamps_include_visible_version_in_skill_and_dist_readme`, `StampHelperTests.test_stamp_md_version_updates_the_single_marker`, `test_stamp_md_version_requires_exactly_one_marker`; source: `SKILL.md`, `dist/README.md`, `dev/tools/build.py` |
| CMH-DOC-11 | The shipped plugin `README.md` pitch surfaces two value props of existing behavior: that comments persist in the browser's `localStorage` and survive a browser restart or reboot while iterating, and that `Copy all` returns every comment at once so the agent makes one coordinated, coherent edit across all notes instead of a fragile one-at-a-time pass. | `tests/test_docs_diagrams.py` - `PitchPersistenceAndBatchDocsTests`; source: `README.md` |
| CMH-DOC-12 | The shipped `SKILL.md` states the plugin installs into both Claude Code and the GitHub Copilot CLI (naming the `claude plugin install` / `copilot plugin install commentable-html@urikan-ai-marketplace` commands) and is invokable from each agent's CLI and Desktop app, so the dual-agent support is visible in the discovery doc; the reserved `claude`/`anthropic` words stay out of the front-matter description (CMH-DOC-06). | `tests/test_docs_diagrams.py` - `DualAgentDocsTests.test_skill_states_dual_agent_install_and_cli_and_desktop`, `DualAgentDocsTests.test_skill_frontmatter_description_avoids_reserved_words`; source: `SKILL.md` |
| CMH-DOC-13 | The shipped plugin `README.md` states the plugin installs into both Claude Code and the GitHub Copilot CLI and shows each agent's marketplace-add and install commands (`claude plugin install` / `copilot plugin install commentable-html@urikan-ai-marketplace`), so the shipped README presents the dual-agent story like the `SKILL.md`, the marketplace manifests, and the site. | `tests/test_docs_diagrams.py` - `PluginReadmeDualAgentTests.test_plugin_readme_states_dual_agent_install`; source: `README.md` |
| CMH-DOC-14 | The shipped skill carries the MIT `LICENSE` at its root (matching the repository root `LICENSE`), so every distributed copy - the Copilot/Claude `plugin install` source subtree and the Claude Desktop skill ZIP - includes the required license notice. | `tests/test_docs_diagrams.py` - `ShippedLicenseTests.test_skill_ships_the_mit_license`, `ShippedLicenseTests.test_shipped_license_matches_the_repo_license`; source: `skills/commentable-html/LICENSE` |
| CMH-DOC-15 | Every `tools/...` path mentioned in the shipped plugin `README.md` resolves to a real shipped file under `skills/commentable-html/tools/`, so tool-bucket refactors cannot leave stale authoring or validation paths in the README. | `tests/test_docs_diagrams.py` - `PluginReadmeToolPathDocsTests.test_plugin_readme_tool_paths_resolve`; source: `README.md`, `skills/commentable-html/tools/` |
| CMH-DOC-16 | The shipped Chart.js reference is split so chart embedding/tooltips and chart recipes/data hygiene/dark theme live in separate focused files, while `references/charts.md` remains only a thin compatibility router. `SKILL.md` links directly to all three files so agents can load the narrow guide they need instead of the old combined reference. | `tests/test_docs_diagrams.py` - `ChartReferenceSplitDocsTests`; source: `SKILL.md`, `references/charts.md`, `references/charts-embedding.md`, `references/charts-recipes.md` |
| CMH-DOC-17 | The shipped `SKILL.md` opens (above the detailed Steps) with a single upfront `## Capabilities and tool index` table that names the tested tool/contract for every capability - create/retrofit/upgrade (`new_document.py`, `retrofit.py`, `upgrade.py`), the `report`/`plan`/`slides`/`board`/`generic` kinds, the review surface (incl. per-section `mark_reviewed.py` tracking and `mark_handled.py` pruning), highlighted code, runnable KQL, diffs, mermaid (`fix_skip.py`), charts, images, layout tools (`generate_toc.py`, `wrap_sections.py`, `doc_stats.py`), layered checklists and editable notes fields (scaffold + `_apply`), commentable widgets, animated decks (`deck_scaffold.py`), output modes, and theming (including deck theme presets via `deck_theme.py`) - and directs the agent to use the named tool and never invent a novel mechanism, plus an upfront `## Always validate before handoff (MUST)` directive naming `tools/authoring/finalize.py <file> [--toc --fix-skip --inline-images --images-base DIR] --strict`, `tools/validate/validate.py --strict <file.html>`, and (for decks) `tools/deck/deck_validate.py --strict`, so an agent cannot miss a capability (notes fields especially) or the mandatory pre-handoff validation on a quick read. | `tests/test_docs_diagrams.py` - `CapabilitiesOverviewDocsTests`; source: `SKILL.md` |
| CMH-DOC-18 | The trimmed shipped `SKILL.md` retains every critical trigger phrase and tested route after consolidating duplicated capability, tool-routing, deterministic-helper, and pre-handoff guidance into one dense tool index. Deep deck workflow detail lives in `references/deck-design.md` and `references/deck-contract.md`, while the entry point keeps only deck triggers, ask-first/design-first/create/validate routing, and pointers. | `tests/test_docs_diagrams.py` - `SkillToolIndexRetentionTests`; source: `SKILL.md`, `references/deck-design.md`, `references/deck-contract.md` |
| CMH-MODE-09 | The NonShareable asset/version banner has an accessible `Dismiss` button. Dismissal hides the banner and persists in `localStorage` by document key plus page/runtime version pair, so reloads keep the same warning hidden while a different version pair can still warn. | `tests/07-nonshareable.spec.js` - `dismissing a version banner hides it across reloads for that version pair`; source: `assets/js/` - `revealAssetBanner()`, `assetBannerDismissKey()` |

## Export security invariants

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-EXP-14 | The whole-document `DOMParser -> doc.documentElement.outerHTML` round-trip used in the four export/apply paths (`36-checklist.js`, `37-notes.js`, `65-export-shareable.js`, `68-export-offline.js`) is safe only because comment/state data never enters it as markup and host HTML is a trusted, unsanitized boundary (CMH-SEC-01). Every `parseFromString()` call site feeds only the trusted document `html` string, never a comment `note`, `quote`, `author`, or any other user-supplied field. If host-HTML sanitization is ever added upstream, this round-trip must be re-audited for serialization-differential stored XSS before landing. | `tests/test_export_domparser_guard.py` - `test_domparser_call_sites_never_take_comment_fields`, `test_domparser_call_sites_use_the_trusted_html_variable`, `test_domparser_present_only_in_expected_partials`; source: `assets/js/36-checklist.js` - `_applyChecklistStateToHtml()`, `assets/js/37-notes.js` - `_applyNoteStateToHtml()`, `assets/js/65-export-shareable.js` - `_applyWidgetLayoutToHtml()`, `assets/js/68-export-offline.js` - `_offlineDocFromHtml()` |
| CMH-EXP-16 | The embedded-comments block and the layer descriptor beside it are resolved as INFRASTRUCTURE, never by scanning document text: a tag walk that follows the HTML tokenizer (it consumes comments - including the empty `<!-->` form and a `--!>` terminator - DOCTYPEs, bogus declarations and processing instructions; skips the TEXT content of raw-text elements, ends one only on an exact end tag, and follows the script-data escaped and double-escaped states behind the `<!--<script>` idiom; treats `<template>` content as the inert fragment `getElementById` cannot see, nesting included; and tokenizes tag names and attributes on ASCII whitespace only) finds the first `<script>` whose PARSED `id` matches, and the result is CROSS-CHECKED against the browser's own parse of the same source before anything is spliced by offsets. The layer's own source is part of every document and necessarily spells that markup, so a text scan was answered by the runtime itself - the "make sure the EMBEDDED COMMENTS region is present" guard could never fire, a document that had genuinely lost the region silently exported a copy whose runtime source had been overwritten mid-function with the comments JSON, and the same defect in the descriptor path overwrote a quarter of a megabyte of runtime on every Offline export of such a document. A document missing the block now fails loudly (Save, Shareable, Standalone, and Offline alike) and downloads nothing; a mention that exists only inside content a browser parses as text never resolves; a shape the walk and the parser cannot agree on resolves nothing rather than being spliced on a guess; and `_getBaseHtml` accepts a fetched on-disk copy only when a real block resolves in it, falling back to the DOM snapshot otherwise. | `tests/57-embedded-comments-guard.spec.js` - `Export as Shareable fails loudly when the embedded-comments block is missing (CMH-EXP-16)`, `Export Offline fails loudly too when the embedded-comments block is missing (CMH-EXP-16)`, `the embedded-comments block is resolved structurally, never from text a browser parses as content (CMH-EXP-16)`, `a fetched copy is accepted only when it really carries the block (CMH-EXP-16)`, `a document that lost its layer descriptor never has its runtime overwritten (CMH-EXP-16)`; source: `assets/js/65-export-shareable.js` - `_cmhForEachTag()`, `_cmhVerifiedScriptRange()`, `_buildSavedHtml()`, `_getBaseHtml()`, `_retargetLayerDescriptor()` |

## Export to Markdown

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-MD-01 | `Export to Markdown` deterministically converts `#commentRoot` content to GFM (headings, tables, fenced code with language, mermaid source not SVG, lists, blockquotes, image figures, KQL kusto fences with the run link), appends live comments as a `Review comments` section (thread-aware: each thread is one heading whose count is per-thread, carrying the root author, then its reply refinements labelled `_Reply k - by NAME:_` oldest-first, with Unicode line separators folded inside a note), and downloads a `.md` file (download only, never writing the clipboard), and is reachable from the sidebar and the overflow menu. | `tests/43-export-markdown.spec.js` - kitchen-sink structure, determinism, callout/chart/diff forms, mermaid source, comment appendix, `the Review comments appendix is thread-aware: root author, ordered reply refinements, and a per-thread count (CMH-MD-01)`, `Export to Markdown downloads a .md file and does NOT write the clipboard`, sidebar/overflow availability; source: `assets/js/` - `htmlToMarkdown()`, `buildMarkdownDoc()`, `exportMarkdown()` |
| CMH-MD-02 | Markdown export is injection-safe: prose, comment notes, and attribute-derived labels cannot forge headings, blockquotes, lists, thematic rules, setext underlines, GFM tables, or raw HTML; link/image labels and URLs are escaped; `javascript:`/control-char URLs are neutralized; code spans and fences stay intact against backtick and pipe tricks. | `tests/43-export-markdown.spec.js` - `plain prose cannot forge headings, blockquotes, lists, or rules`, `a comment note cannot forge a setext heading`, `a comment note cannot forge a GFM table`, `a backslash in link text cannot hijack the destination`, `a link URL with a leading control char cannot smuggle an executable scheme`, `pipes inside code spans in a table cell stay escaped, not column boundaries`, `a code language class with a backtick cannot void the fence`, `a comment note cannot forge headings/fences or inject HTML in the appendix`, `attribute-derived labels (image alt) escape HTML and markup`; source: `assets/js/` - `_mdText()`, `_mdEscapeLeading()`, `_mdEscapePipes()`, `_mdUrl()` |
| CMH-MD-03 | List items serialize inline `<strong>`/`<em>`/`<a>`/`<code>`/`<img>` that are DIRECT children of the `<li>` (not only inside a nested block), so a bullet mixing `**bold**`, an inline `<a>` link, and `code` keeps its emphasis, link, and code span in the exported Markdown. | `tests/47-hardening.spec.js` - `Markdown export serializes strong/link/code that are direct list-item children (CMH-MD-03)`; source: `assets/js/` - `_mdInlineOne()`, `_mdAppendInline()`, `_mdList()` |
| CMH-MD-04 | The Markdown URL allowlist keeps only image data URLs: a bare `data:` destination (for example `data:text/html`) is neutralized to `about:blank`, while a `data:image/...` source is preserved so inline images still export. | `tests/47-hardening.spec.js` - `Markdown export drops a bare data: URL but keeps a data:image URL (CMH-MD-04)`; source: `assets/js/` - `_mdUrl()` |
| CMH-MD-05 | Markdown export serializes `data-cm-widget` content even when the widget is `cm-skip`: slotted boards export as a `_[Widget: ...]_` note plus a GFM table whose columns are `data-cm-slot` values and whose cells are `data-cm-part-label` values; unslotted widgets export as a bullet list. | `tests/43-export-markdown.spec.js` - `expanded demo item kinds convert to Markdown`; source: `assets/js/` - `_mdWidget()` |
| CMH-MD-06 | Markdown export deliberately omits `data-cm-offline-chart` image payloads to avoid base64 bloat, emitting only the offline chart snapshot's caption or alt text as a chart-snapshot placeholder. | `tests/43-export-markdown.spec.js` - `offline chart snapshots export label text without embedding the data image (CMH-MD-06)`; source: `assets/js/` - `_mdFigure()` |
| CMH-MD-07 | The Markdown review-comments appendix carries an agent-facing untrusted-data boundary: read-first instructions frame every reviewer note as a document-scoped request rather than an agent or system instruction, and each root/reply note is individually enclosed in a dynamic tilde-sized `BEGIN/END UNTRUSTED REVIEWER NOTE` fence whose delimiter exceeds every tilde run in that note. Bidi/invisible formatting controls are stripped before note text enters a fence. | `tests/43-export-markdown.spec.js` - `the Markdown appendix marks reviewer notes as untrusted fenced data (CMH-MD-07)`, `the Markdown appendix strips bidi controls from reviewer notes (CMH-MD-07)`; source: `assets/js/60-export-markdown.js` - `_mdCommentsAppendix()` |
| CMH-MD-08 | A `<blockquote>` serializes its block children recursively (paragraphs, lists, code fences, nested blockquotes) so every output line is prefixed with `> `, preserving the multi-block structure. The old `_mdCollapse(_mdInlineText(el))` path that flattened all children to one line is not used. | `tests/43-export-markdown.spec.js` - `a blockquote with nested block children preserves each child on its own > prefixed lines (CMH-MD-08)`; source: `assets/js/` - `_mdBlock()` BLOCKQUOTE branch |
| CMH-PORT-01 | `tools/authoring/to_shareable.py` migrates an existing NonShareable document into a self-contained Shareable one, preserving its authored CONTENT, its embedded comments, and its handled ids - the whole point of migrating rather than regenerating is that review state travels with the document. It is the counterpart to `upgrade.py`, which deliberately REFUSES a NonShareable document. It inlines the COMPANION FILES `commentable-html.css` and `commentable-html.js` rather than copying the Shareable template's CSS/JS region inners: those inners carry their own `BEGIN/END: commentable-html - CSS` banner text, so copying them left the migrated file with three copies of a region marker and it failed validation. `commentable-html.assets.js` is DROPPED, not inlined: it is a registry holding a second full copy of the CSS and JS (about 937 KB) that exists only so the in-page export can do this same conversion, so a migrated document does not need it and a natively generated Shareable one does not carry it. A companion is matched by the BASENAME of the URL its element references, so the absolute `file://` refs the CLI produced by default, `--assets-relative` / `--copy-assets` / `--assets-href` prefixes, and `?v=` cache-busters all migrate; CRLF documents migrate and keep their line endings. The legacy scaffolding (bootstrap block, loader note) is removed from the document's OWN bytes before any companion text is inlined, and only outside CONTENT, so neither authored prose nor untrusted companion text can forge those anchors. An already-Shareable document is returned byte-identical, so the tool is idempotent and safe to run across a mixed directory, and a file that is not a commentable-html document is refused rather than rewritten. | `tests/test_to_shareable.py` - `MigrationTests` (content, embedded comments and handled ids preserved; self-contained with no companion references left; the registry dropped rather than inlined; idempotent; the result validates; the CLI migrates in place and refuses a foreign file), `LegacyReferenceShapeTests`, `LineEndingTests`, `AuthoredScaffoldingTests`, `BatchCliTests` |
| CMH-PORT-02 | A NonShareable document is opened, validated and finalized PERMANENTLY - there is no deprecation deadline. The NonShareable runtime and its companions (`NONSHAREABLE.html`, `commentable-html.{css,js,assets.js}`) stay shipped in `dist/` for exactly that reason: existing documents reference them by bare name and would break on the next auto-update if they were dropped. `finalize` must not silently convert such a document either. | `tests/test_to_shareable.py` - `PermanentCompatibilityTests` (a NonShareable document still validates; the runtime and all three companions are still shipped; finalize works and leaves the mode alone) |
| CMH-PORT-03 | Shareable is the ONLY mode generated, by every creation route. `new_document.py` follows the RESOLVED TEMPLATE rather than a flag, so the two can never disagree: the default is `dist/SHAREABLE.html`, and a caller that genuinely needs a legacy NonShareable document (chiefly the compatibility suite) asks for it explicitly with `--template <dist>/NONSHAREABLE.html`, which still gets the full companion-reference handling. `retrofit.py` likewise produces a Shareable document by default; a legacy one requires an explicit `--nonshareable` or one of the companion-href options (`--assets-relative`, `--copy-assets`, `--assets-href`), which only make sense in that mode. `deck_scaffold.py` was already Shareable-only. The old mode flags stay accepted so existing callers do not break: `--nonshareable` on `new_document.py` is ignored, and `--shareable` on either tool now names the default. Because `--template` is also the standard way to reach NonShareable, "a template was passed" no longer implies "a CUSTOM template": the skill's own `NONSHAREABLE.html` still gets its companion references repointed and existence-checked, while a genuinely foreign template remains the caller's responsibility. | `tests/test_new_document.py` - `test_the_default_mode_is_now_shareable`, `test_an_explicit_nonshareable_template_still_builds_one`, `test_explicit_nonshareable_refs_resolve_to_dist`, and the surrounding `NonShareableCliTests` companion-mode cases; `tests/test_retrofit.py` - `test_the_default_retrofit_is_shareable`, `test_shareable_is_accepted_as_a_deprecated_no_op`, `test_an_explicit_nonshareable_retrofit_still_builds_one` |
| CMH-PORT-04 | Migration is fail-safe on hostile or ambiguous input, because it rewrites the user's only copy of a document in place. (a) Companion bytes are inlined with any raw-text terminator for their host element escaped, so a `--dist` whose stylesheet carries `</StYlE><script>` cannot turn control of a stylesheet into script execution; the payload is neutralized, never silently dropped. (b) The write is staged in the target's directory and swapped in with `os.replace` (shared `_atomic_io.atomic_write`, which also follows a symlink to its target and clears a read-only staged file so cleanup cannot leak it), so an interrupted or failing write leaves the original intact instead of truncating it to zero bytes. (c) EVERY anchor - the CONTENT span, the bootstrap block, the loader note, each companion element and the descriptor - is resolved against the document's ORIGINAL bytes and applied as one pass of non-overlapping edits, so untrusted companion text can never aim a later step; and candidates are found in a view with HTML comments and raw-text bodies blanked, so a commented-out or quoted copy of an element is never the one rewritten. (d) Ambiguity is refused rather than guessed: a duplicated CONTENT marker, more than one live descriptor, or more than one live reference to the same companion aborts before anything is written, and a result that would still reference a companion outside CONTENT is refused too. (e) The mode transition parses the layer descriptor as JSON and requires exactly one nonshareable -> shareable transition, so a reformatted descriptor can no longer be reported as migrated while still marked nonshareable, and a document with no parsable descriptor is refused. | `tests/test_to_shareable.py` - `HostileLayerTests`, `SafeWriteTests`, `ContentMarkerIntegrityTests`, `DescriptorTests`, `LiveMarkupOnlyTests`, `AuthoredScaffoldingTests`; `tests/48-to-shareable.spec.js` - `a migrated document opens, comments and copies like a native Shareable one (CMH-PORT-04)`, `a hostile dist cannot execute script in a migrated document (CMH-PORT-04)` |
| CMH-PORT-05 | The "Portable" concept is named **Shareable** everywhere a reader can see it - the runtime buttons and tooltips (`Export as Shareable`, `Shareable`), the shareability badge (`Shareable` / `Not shareable` / `Offline`), the Help/About panel, the toasts, the skill references, `SKILL.md`, the tutorial, the demo reports and showcase deck, and the marketplace site pages - and `NonPortable` is named `NonShareable`. The rename reaches the internal identifiers too: the layer descriptor emits `"mode": "shareable"` / `"nonshareable"`, the dist templates are `SHAREABLE.html` / `NONSHAREABLE.html`, the companion bootstrap anchor is `NONSHAREABLE BOOTSTRAP`, `new_document.py` / `retrofit.py` take `--shareable` / `--nonshareable`, the migration tool is `tools/authoring/to_shareable.py`, and the validator's symbols and diagnostics read `nonshareable mode: ...`. | `tests/test_shareable_compat.py` - `LegacyDescriptorModeTests.test_new_shareable_mode_validates_clean`, `test_new_nonshareable_mode_validates_clean`, `LegacyTemplateNameTests.test_build_emits_the_new_template_names`, `LegacyToolPathTests.test_to_shareable_exists`, `LegacyCliFlagTests`; `tests/07-nonshareable.spec.js`; `tests/48-to-shareable.spec.js`; `tests/34-modes.spec.js` |
| CMH-PORT-06 | A document produced BEFORE that rename keeps validating with NO new error and NO new warning: the validator accepts the legacy layer-descriptor modes (`portable` / `nonportable`) as exactly equivalent to the current ones, and `to_shareable.py` reads them the same way, so an already-shipped file neither has to be migrated nor re-validated. An unknown mode is still rejected. | `tests/51-legacy-compat.spec.js` - `a legacy companion document loads, reports its mode, and honors legacy companion-only controls`; `tests/test_shareable_compat.py` - `LegacyDescriptorModeTests.test_legacy_single_file_mode_validates_clean`, `test_legacy_companion_mode_and_anchor_validate_clean`, `test_a_single_file_document_still_rejects_a_companion_mode`, `test_an_unknown_mode_is_still_rejected`, `LegacyBootstrapAnchorTests.test_to_shareable_reads_the_legacy_descriptor_mode`; `tests/test_upgrade_corpus.py` (the shipped pre-rename single-file snapshot) |
| CMH-PORT-07 | The legacy companion bootstrap anchor (`NONPORTABLE BOOTSTRAP`), which is baked verbatim into every already-shipped companion document, stays recognized by `upgrade.py`, `new_document.py`, `retrofit.py`, `to_shareable.py`, and the in-page Plain / standalone exports - so a legacy document still upgrades, migrates, and exports to a single file. `to_shareable.py` also accepts a MIXED anchor pair (a legacy BEGIN with a current END, or the reverse), which a hand-edited or partially-migrated document can carry, rather than silently leaving a dead companion watchdog behind - but only when the block is WELL FORMED: an END that belongs to a later block (a second BEGIN falls between the two anchors) is refused rather than deleting the authored content in between. The pre-rename public constant `NONPORTABLE_MARKER` also stays exported by `upgrade.py` and `new_document.py`, aliasing the legacy anchor string it always held. The in-page strippers accept either spelling but require the SAME one at both ends (a backreference), and the Plain export only strips in companion mode - so accepting a second anchor spelling can never make an export delete an authored quotation of it from a self-contained document. The pre-rename `-portable` export-filename suffix is still stripped, so re-exporting a file an earlier release produced does not stack suffixes. The runtime and the stylesheet also keep the legacy `cm-nonportable` / `cm-nonportable-only` hooks beside the current ones - defensively: that class is applied at RUNTIME rather than baked into a document, so nothing shipped depends on it, but a retrofitted host page or a hand-written stylesheet may still key off the old name - and a pre-rename companion document's `Export as Portable` aria-label is re-stamped along with its visible label. | `tests/test_shareable_compat.py` - `LegacyBootstrapAnchorTests` (`test_upgrade_recognizes_both_anchors`, `test_new_document_recognizes_both_anchors`, `test_to_shareable_strips_a_legacy_anchor`, `test_to_shareable_strips_a_mixed_anchor_pair`, `test_to_shareable_refuses_to_strip_across_an_intervening_begin`, `test_the_legacy_marker_constant_name_survives`, `test_runtime_strips_both_anchors`), `LegacyRuntimeClassTests`, `LegacyExportFilenameTests`; `tests/51-legacy-compat.spec.js` - `exporting a legacy companion document strips the legacy anchor and yields one valid shareable file`, `a self-contained document that QUOTES a legacy bootstrap anchor keeps it through Plain export` |
| CMH-PORT-08 | The tools resolve a dist template by its CURRENT name first and fall back to the pre-rename name (`SHAREABLE.html` then `PORTABLE.html`; `NONSHAREABLE.html` then `NONPORTABLE.html`), so an older checkout, a vendored stage, or a partially-updated install still works; when neither exists the CURRENT name is reported, so the error names the file a fresh build produces. The mapping also runs in REVERSE for a caller-supplied `--template`: a script that still passes `<dist>/PORTABLE.html` (or `NONPORTABLE.html`) to `new_document.py`, `upgrade.py`, or `vendored_libs.py` is pointed at the current sibling instead of failing to read a renamed file, while an existing path and any other custom template are never redirected. | `tests/test_shareable_compat.py` - `LegacyTemplateNameTests.test_resolver_prefers_the_new_name`, `test_resolver_falls_back_to_the_legacy_name`, `test_resolver_returns_the_current_name_when_neither_exists`, `test_a_legacy_template_path_resolves_forward`, `test_every_template_taking_tool_resolves_a_legacy_path`, `test_an_existing_or_custom_template_path_is_never_redirected`; source: `tools/_toolpath.py` - `dist_template`, `resolve_template_path` |
| CMH-PORT-09 | The pre-rename CLI and import surface keeps working: `--portable` / `--nonportable` stay accepted aliases of `--shareable` / `--nonshareable` on `new_document.py` and `retrofit.py`; `tools/authoring/to_portable.py` stays as a thin alias that forwards argv to `to_shareable.py` and re-exports its module API (including the legacy `is_nonportable` name), printing a one-line deprecation note to stderr so an existing script or recipe does not break; and `tools/validate/validate.py` - which the authoring tools import by bare name - keeps its pre-rename symbols (`NONPORTABLE_REGIONS`, `DEMO_NONPORTABLE_*`, `_is_nonportable`, `_check_nonportable`, `_nonportable_*_refs`) as aliases of the current objects. The validator and `to_shareable.py` must also agree on the accepted mode spellings. | `tests/test_shareable_compat.py` - `LegacyCliFlagTests.test_new_document_accepts_both_spellings`, `test_retrofit_accepts_both_spellings`, `LegacyToolPathTests.test_legacy_shim_exists_and_runs`, `test_legacy_shim_reports_deprecation_on_stderr`, `test_legacy_shim_re_exports_the_module_api`, `LegacyCliFlagTests.test_the_legacy_flag_still_produces_the_same_document_as_the_current_one`, `test_the_legacy_companion_flag_is_still_accepted_and_ignored`, `test_the_two_legacy_flags_stay_mutually_exclusive`, `test_retrofit_accepts_the_legacy_flag_end_to_end`, `LegacyValidatorApiTests.test_module_exposes_the_legacy_names`, `test_the_accepted_mode_spellings_agree_across_the_validator_and_the_migration_tool` |
