## Packaging and first-run extraction

The plugin ships a compact `skills/commentable-html/` (only `SKILL.md`, `LICENSE`, and a single `skill-resources.zip`) so the CLI installer writes very few files. This matters on Windows, where Defender briefly locks each file as it is written and the installer aborts the whole install on the first transient lock (the `Access is denied. (os error 5)` failure) - a large file count almost never gets through. The bulky runtime (`tools/`, `references/`, `dist/`, `vendor/`) is assembled by `build.py` into `skill-resources.zip`, and a SessionStart hook unpacks it on first run per version, retrying the same transient lock the installer does not. The large tutorial (`docs/`) and worked examples (`examples/`) are NOT shipped; they live at the plugin top level and online. The editable + built skill STAGE tree lives at `dev/skill/`.

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-PKG-01 | The extractor unpacks every member of `skill-resources.zip` into the skill dir and writes a version-stamped `.skill-resources-<version>.ok` marker only after all members succeed. | `tests/test_extract_resources.py` - `test_first_extraction_unpacks_all_members_and_writes_marker` |
| CMH-PKG-02 | A present version marker makes a re-run a no-op: no member is extracted (the hot path does no work). | `tests/test_extract_resources.py` - `test_present_marker_makes_rerun_a_noop`, `test_missing_zip_is_a_soft_noop` |
| CMH-PKG-03 | A transient file lock (Defender: `PermissionError` / `WinError 5/32/33` / `errno EACCES/EBUSY`) is retried per-member with exponential backoff, within an overall time budget, until it clears; the zip OPEN itself is retried too (the fresh zip is what Defender scans). | `tests/test_extract_resources.py` - `test_transient_lock_is_retried_until_success`, `test_retries_winerror_and_errno_lock_variants`, `test_winerror_lock_is_retried` |
| CMH-PKG-04 | An unrecoverable lock (retries/budget exhausted) leaves NO marker, so the next SessionStart re-extracts (self-heal); a partial extraction never looks complete; a non-lock error is not retried. | `tests/test_extract_resources.py` - `test_unrecoverable_lock_leaves_no_marker`, `test_non_lock_error_is_not_retried` |
| CMH-PKG-05 | A stale marker from an older version is cleared and the current version's marker is written, and files removed/renamed in the new version do not linger (the owned runtime dirs are wiped before extraction), so an update re-extracts cleanly. | `tests/test_extract_resources.py` - `test_stale_marker_is_cleared_and_new_one_written`, `test_removes_files_absent_from_new_version` |
| CMH-PKG-06 | `--force` re-extracts even when the current marker is present. | `tests/test_extract_resources.py` - `test_force_reextracts_even_with_marker_present` |
| CMH-PKG-07 | The shipped skill dir is minimal - only `SKILL.md`, `LICENSE`, and `skill-resources.zip`. | `tests/test_session_hooks.py` - `test_shipped_skill_dir_is_minimal` |
| CMH-PKG-08 | `skill-resources.zip` carries exactly the runtime dirs (`tools/`, `references/`, `dist/`, `vendor/`) and neither the tutorial (`docs/`) nor the examples; it carries no `__pycache__`/`*.pyc` junk and unpacks to a working tools tree. A traversing/absolute zip member is rejected (fail closed) on top of stdlib zip-slip sanitisation. | `tests/test_session_hooks.py` - `test_zip_carries_runtime_dirs_but_not_tutorial_or_examples`, `test_zip_extracts_to_a_working_tools_tree`; `tests/test_extract_resources.py` - `test_rejects_zip_slip_member` |
| CMH-PKG-09 | Both hook configs do a version-stamped marker existence check on the HOT PATH before ever invoking Python; they probe the interpreter and skip the Windows Store `python3` alias stub, run it isolated (`-I`), and the PowerShell cold path forces a success exit (non-blocking). `plugin.json` wires the Copilot hook. | `tests/test_session_hooks.py` - `test_copilot_hook_checks_marker_before_python`, `test_claude_hook_checks_marker_before_python`, `test_hooks_probe_interpreter_and_skip_windowsapps_stub`, `test_powershell_hook_forces_success_exit`, `test_plugin_json_wires_the_copilot_hooks` |
| CMH-PKG-10 | The hook marker version is stamped to `dev/VERSION` with no stale marker, and the extractor script ships next to the hooks. | `tests/test_session_hooks.py` - `test_hook_marker_version_matches_dev_version`, `test_extractor_ships_next_to_the_hooks` |
| CMH-PKG-11 | `build.py` assembles `skill-resources.zip` deterministically (sorted members, fixed timestamp/mode) and `--check` (via `check_package`) catches drift in the zip contents, the shipped SKILL.md/LICENSE, and the hook version stamps, reports a corrupt zip, and refuses to package a symlinked build input (supply-chain guard). | `tests/test_build.py` - `test_resources_zip_is_deterministic`, `test_package_check_detects_zip_drift`, `test_package_check_detects_hook_stamp_drift`, `test_check_package_reports_corrupt_zip`, `test_packager_rejects_a_symlinked_input` |
| CMH-PKG-12 | Concurrent session startups do not interleave extraction: an exclusive lock serializes them (a fresh lock makes the second session skip), and a lock abandoned by a crashed process is stolen after a timeout so extraction can never be blocked forever. | `tests/test_extract_resources.py` - `test_concurrent_lock_makes_run_skip`, `test_stale_lock_is_stolen` |
