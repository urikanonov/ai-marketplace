## Packaging and first-run extraction

The plugin ships a compact `skills/commentable-html/` (only `SKILL.md`, `LICENSE`, and a single `skill-resources.zip`) so the CLI installer writes very few files. This matters on Windows, where Defender briefly locks each file as it is written and the installer aborts the whole install on the first transient lock (the `Access is denied. (os error 5)` failure) - a large file count almost never gets through. The bulky runtime (`tools/`, `references/`, `dist/`, `vendor/`) is assembled by `build.py` into `skill-resources.zip`, and a SessionStart hook unpacks it on first run per version, retrying the same transient lock the installer does not. The large tutorial (`docs/`) and worked examples (`examples/`) are NOT shipped; they live at the plugin top level and online. The editable + built skill STAGE tree lives at `dev/skill/`.

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-PKG-01 | The extractor unpacks every member of `skill-resources.zip` into the skill dir and writes a version-stamped `.skill-resources-<version>.ok` marker only after all members succeed. | `tests/test_extract_resources.py` - `test_first_extraction_unpacks_all_members_and_writes_marker` |
| CMH-PKG-02 | A present version marker makes a re-run a no-op: no member is extracted (the hot path does no work). | `tests/test_extract_resources.py` - `test_present_marker_makes_rerun_a_noop`, `test_missing_zip_is_a_soft_noop` |
| CMH-PKG-03 | A transient file lock (Defender: `PermissionError` / `WinError 5` / `errno EACCES/EBUSY`) is retried per-member with exponential backoff until it clears. | `tests/test_extract_resources.py` - `test_transient_lock_is_retried_until_success` |
| CMH-PKG-04 | An unrecoverable lock (retries exhausted) leaves NO marker, so the next SessionStart re-extracts (self-heal); a partial extraction never looks complete. | `tests/test_extract_resources.py` - `test_unrecoverable_lock_leaves_no_marker` |
| CMH-PKG-05 | A stale marker from an older version is cleared and the current version's marker is written, so an update re-extracts the new payload. | `tests/test_extract_resources.py` - `test_stale_marker_is_cleared_and_new_one_written` |
| CMH-PKG-06 | `--force` re-extracts even when the current marker is present. | `tests/test_extract_resources.py` - `test_force_reextracts_even_with_marker_present` |
| CMH-PKG-07 | The shipped skill dir is minimal - only `SKILL.md`, `LICENSE`, and `skill-resources.zip`. | `tests/test_session_hooks.py` - `test_shipped_skill_dir_is_minimal` |
| CMH-PKG-08 | `skill-resources.zip` carries exactly the runtime dirs (`tools/`, `references/`, `dist/`, `vendor/`) and neither the tutorial (`docs/`) nor the examples; it carries no `__pycache__`/`*.pyc` junk and unpacks to a working tools tree. | `tests/test_session_hooks.py` - `test_zip_carries_runtime_dirs_but_not_tutorial_or_examples`, `test_zip_extracts_to_a_working_tools_tree` |
| CMH-PKG-09 | Both hook configs (Copilot `hooks.json` sessionStart and Claude `hooks/hooks.json` SessionStart `startup|resume`) do a version-stamped marker existence check on the HOT PATH before ever invoking Python, and `plugin.json` wires the Copilot hook. | `tests/test_session_hooks.py` - `test_copilot_hook_checks_marker_before_python`, `test_claude_hook_checks_marker_before_python`, `test_plugin_json_wires_the_copilot_hooks` |
| CMH-PKG-10 | The hook marker version is stamped to `dev/VERSION` with no stale marker, and the extractor script ships next to the hooks. | `tests/test_session_hooks.py` - `test_hook_marker_version_matches_dev_version`, `test_extractor_ships_next_to_the_hooks` |
| CMH-PKG-11 | `build.py` assembles `skill-resources.zip` deterministically (sorted members, fixed timestamp/mode) and `--check` (via `check_package`) catches any drift in the zip contents or the shipped SKILL.md/LICENSE/hook stamps. | `tests/test_build.py` - `test_resources_zip_is_deterministic`, `test_package_check_detects_zip_drift` |
