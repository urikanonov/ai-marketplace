## Deck integration (frontend-slides)

The commentable-html skill vendors a curated, pristine subset of the third-party frontend-slides skill (MIT, Zara Zhang) under `pkg/skills/commentable-html/vendor/frontend-slides/` to power a built-in deck capability. The vendored subtree is enforced pristine by a required CI gate. The author-time deck tools live under `pkg/skills/commentable-html/tools/deck/`, and the runtime deck profile lives in the layer (activated only by `data-cmh-mode="deck"`, so non-deck documents are unaffected). See `references/deck-contract.md` for the runtime interface contract.

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| CMH-DECK-01 | The skill detects a presentation/deck request ("slide deck", "presentation", "pitch deck", "convert this ppt") and CONFIRMS with the user before building a real deck rather than silently producing a flat document; if the user declines it falls back to a normal flat document. This is an agent-behavior convention in `SKILL.md` ("Deck capability"), so its coverage is `manual`. | `manual` - documented in `SKILL.md`; agent-behavior convention, not automatable |
| CMH-DECK-02 | `deck/deck_scaffold.py` produces a valid, create-only, commentable-native fixed-stage deck: `#commentRoot` carries `data-cmh-mode="deck"` and a `commentable-html-kind` of `slides`, the body holds `.deck-viewport > .deck-stage` with `viewport-base.css` inlined, every slide is a `<section class="slide">` with a unique stable `data-slide-id` and the first is `.active`, the deck body has no inline editor / remote fonts / host script, ids are deterministic, and it refuses to overwrite an existing output without `--force`. Before writing it runs BOTH the base validator AND the deck contract (`deck_validate.deck_checks`) and fails closed (writing nothing) on any contract violation; its deck-mode injection targets the real content root (never the template decoy) idempotently. | `tests/test_deck_scaffold.py` - `test_scaffold_is_valid_and_commentable_native`, `test_slides_have_stable_ids_first_active`, `test_deck_body_has_no_editor_fonts_or_script`, `test_content_ids_preserved_and_missing_ones_minted`, `test_deterministic_ids`, `test_create_only_then_force`, `test_fragment_without_slides_errors`, `test_existing_ids_and_inject_targets_real_root`, `test_scaffold_fails_closed_on_remote_media`, `test_scaffold_declares_slides_kind`, `test_scaffold_has_legible_presentation_defaults` |
| CMH-DECK-03a | `deck/pptx_to_fragment.py` HTML-escapes extracted slide text (no `<script>` / `</b>` / `-->` injection; RTL and emoji preserved), emits one `<section class="slide">` per slide with a stable id, ignores speaker notes, and the local `--pptx` fallback fails closed without `python-pptx`. It also schema-validates the extracted JSON (each slide/content-block/image is an object; `content`/`images` are lists), rejects an empty slide list and a slide with no title/text/image, and vets every image `path` as local-relative OR a self-contained `data:image/...` URI (rejecting a remote/other scheme, `//host`, absolute, or `..` traversal; normalizing backslashes; skipping an empty path). The `--pptx` fallback inlines each extracted image as a `data:` URI before its temp dir is deleted, so a converted deck never ships a dangling `assets/...` reference. | `tests/test_pptx_to_fragment.py` - `test_hostile_text_is_escaped`, `test_notes_are_ignored`, `test_structure_and_stable_id`, `test_duplicate_slides_get_distinct_ids`, `test_cli_stdin_to_stdout`, `test_non_list_input_errors`, `test_pptx_fallback_fails_closed`, `test_remote_and_unsafe_image_paths_rejected`, `test_data_image_uri_is_accepted`, `test_local_pptx_inlines_extracted_images_as_data_uri`, `test_inline_local_images_leaves_odd_and_missing_paths`, `test_backslash_image_path_normalized`, `test_empty_image_path_skipped_not_error`, `test_empty_slide_list_rejected`, `test_slide_with_no_content_rejected`, `test_non_dict_slide_rejected`, `test_non_list_content_or_images_rejected`, `test_non_dict_content_block_or_image_rejected`, `test_cli_empty_list_exits_nonzero` |
| CMH-DECK-03b | When a `.pptx` conversion is requested, the flow prefers the Anthropic `pptx` skill (when installed) and falls back to the vendored local extractor. This is prompt-level routing in `SKILL.md`; only the deterministic escaping and the local fallback (CMH-DECK-03a) are automated, so this row's coverage is `manual`. | `manual` - prompt-level wording in `SKILL.md`; the deterministic extraction path is covered by CMH-DECK-03a |
| CMH-DECK-04 | `deck/deck_validate.py` passes a scaffolded deck and fails closed on each deck-contract violation. Active-content and egress checks are HTML-parser based (not regex), so they cannot be bypassed by a solidus attribute separator (`<svg/onload=>`), an entity-encoded scheme (`&#106;avascript:`), an unquoted attribute (`<img src=//evil>`), or an SVG `<image>`/`<use>` `href`. It rejects: missing `data-cmh-mode`, a remote font reference, remote media/resource (`img`/`video`/`audio`/`source`/`track`/`image`/`use`/`iframe`/`embed`/`object`/`link`/`base` fetch attr or CSS `url()` to `http(s):` / `//`), a remote CSS `@import`, an inline event handler, `<iframe>/<object>/<embed>`, an external `<script src/href>` or any `<script>` nested in `<svg>` (an inline chart-init `<script>` is allowed), a `<meta http-equiv=refresh>` redirect, a dangerous URL scheme (`javascript:`/`vbscript:`/`data:text/html`), a `../` asset reference, a `<deck-stage>` component, or a duplicate slide id. It tolerates a local-relative image, a `data:` image URI, an external hyperlink (`<a href>` is not egress), and a local `<link href>`; it does not false-positive on the plain-text token "edit-toggle" in prose. The content region is anchored on the full HTML-comment markers and uses the LAST end marker, so slide text containing the bare marker text cannot truncate validation. It also runs the base validator. | `tests/test_deck_validate.py` - `test_valid_deck_passes`, `test_missing_deck_mode_fails`, `test_remote_font_fails`, `test_event_handler_fails`, `test_iframe_fails`, `test_javascript_url_fails`, `test_parent_traversal_fails`, `test_deckstage_component_fails`, `test_duplicate_slide_id_fails`, `test_cli_passes_on_valid_deck`, `test_remote_media_fails`, `test_remote_css_url_fails`, `test_local_media_and_data_uri_pass`, `test_unquoted_javascript_and_traversal_fail`, `test_edit_toggle_plaintext_is_not_a_false_positive`, `test_solidus_and_whitespace_separated_event_handler_fails`, `test_entity_encoded_javascript_url_fails`, `test_unquoted_remote_media_fails`, `test_svg_image_and_use_remote_href_fails`, `test_html_image_alias_background_and_image_set_egress_fail`, `test_external_hyperlink_is_allowed`, `test_injected_end_marker_does_not_truncate_validation`, `test_begin_marker_without_close_reports_missing_region`, `test_duplicate_active_content_is_deduplicated`, `test_svg_and_external_scripts_fail_inline_chart_script_allowed`, `test_stray_svg_close_and_benign_meta_are_handled`, `test_remote_link_base_and_meta_refresh_fail_local_link_allowed` |
| CMH-DECK-18 | `deck/deck_fix_fonts.py` deterministically removes remote web-font loaders (`<link>`, remote `@import`, and remote `@font-face`) and rewrites deck font stacks to the approved system families: serif/editorial to Iowan, slab/display/script to Impact, geometric/body sans to system-ui, monospace to Cascadia, and explicit CJK remote families to a dropped system fallback. A fixed valid deck passes `deck_validate.py`. | `tests/test_deck_fix_fonts.py` - `test_cmh_deck_18_maps_web_fonts_and_strips_remote_loaders`, `test_cmh_deck_18_cli_writes_deterministic_output` |
| CMH-DECK-19 | `deck/deck_validate.py` emits a non-fatal content overload advisory when a slide or draggable board card exceeds the tunable authored line or element budgets, while keeping normal scaffolded slides clean. The advisory is deck-only and does not touch the flat-document validator. | `tests/test_deck_validate.py` - `test_cmh_deck_19_overloaded_slide_warns_without_error`, `test_cmh_deck_19_normal_slide_has_no_overload_warning`, `test_cmh_deck_19_overloaded_board_card_warns` |
| CMH-DECK-05 | The runtime deck profile (activated by `#commentRoot[data-cmh-mode="deck"]` on the real content root) exposes `window.__cmhDeck` (`showSlide`/`showSlideById`/`activeSlideId`/`slideCount`), scales the fixed 1920x1080 stage to exactly `min(vw/1920, vh/1080)` (refit via a `ResizeObserver` on the viewport, so a window resize or comment-mode inset re-fits it), starts on the first slide and navigates by keyboard (Arrow/Space/PageUp/PageDown/Home/End, gated so a keypress inside an editable field - input/textarea/select/contenteditable/`.cm-skip` - never navigates) / id / a slide-oriented control bar (Prev, a live `N / total` counter with a spoken `aria-label`, Next, with WCAG-2.5.3 aria-labels and Prev/Next disabled at the boundaries), guards `showSlide` against a non-integer or out-of-range index and `showSlideById` against an empty/unknown id, dispatches `cmh:slidechange` only on a CHANGED active slide (never at setup, on a re-selection of the active slide, or on an invalid call), installs its chrome exactly once (idempotent) with the toggle first and the nav bar last in focus order, suppresses the flow-document chrome (heading anchors, collapsible carets, side TOC, footer, scroll progress), and defaults to a full-screen presentation (**present mode** hides the comment sidebar/toolbar and suppresses the selection popup AND the document context menu while preserving the native right-click); a **comment mode** toggle (stable accessible name + `aria-pressed`) reveals the sidebar, insets the stage so the sidebar does not overlap it (nav bar re-centred on the reduced stage), force-reveals animated content, and gates navigation keys; a comment card jumps to (activates) its owning slide (deck-aware jump, resolving text and non-text `[data-cids~]` anchors, and a document-wide comment jumps to slide 1) with highlights restoring on hidden slides across reload. The deck present-mode body class is stripped on export, and a deck survives Export Offline (mode, comments on hidden slides, and the card jump all round-trip with zero network). The sidebar's empty state carries a deck-specific hint on how to comment on a live slide; on a narrow screen the deck controls yield to the open comment sidebar. Non-deck documents are unaffected (the profile is fully gated on the deck signal). | `tests/52-deck.spec.js` - `CMH-DECK-05: exposes the controller and starts on the first slide`, `CMH-DECK-05d: keyboard, id, and prev/next navigation; doc-mode chrome suppressed`, `CMH-DECK-05d: typing in an editable field does not navigate; deck chrome is installed once`, `CMH-DECK-05d: the deck-aware jump resolves a non-text ([data-cids~]) anchor's slide`, `CMH-DECK-05d: cmh:slidechange fires only for a changed active slide`, `CMH-DECK-05: the stage refits on viewport resize and comment mode narrows it`, `CMH-DECK-05: slides fit the fixed 1080px stage and vertical overflow is detectable as clipping`, `CMH-DECK-05a: present mode hides the comment UI; comment mode reveals it and gates keys`, `CMH-DECK-05a: present mode suppresses the doc-comment menu and keeps the native menu; comment mode restores it`, `CMH-DECK-05a: on a narrow screen the deck controls yield to the open sidebar`, `CMH-DECK-05a: comment mode force-reveals staged slide content`, `CMH-DECK-05c/05d: a comment restores on a hidden slide and its card jumps to that slide`, `CMH-DECK-05c: a comment on a hidden slide restores after reload`, `CMH-DECK-05: a REAL scaffolded deck activates the runtime, not the flow-document chrome`, `CMH-DECK-05: Export Offline round-trips a deck (mode + comments + card jump) with zero network` |
| CMH-DECK-06 | Deck mode includes a split-screen slide-overview navigator: the nav-bar toggle and `o` shortcut open a grid with one thumbnail button per slide, each button carries the slide title as its tooltip and accessible label, click or Enter/Space jumps to that slide and closes the grid, Escape closes it, arrow/Home/End keys move focus inside the grid, overview clones are hidden from keyboard focus, the lazy overview is stripped from exports, and the same overview works in present and comment modes. | `tests/52-deck.spec.js` - `CMH-DECK-06: overview grid opens, lists every slide, titles slides, and click-jumps`, `CMH-DECK-06: overview keyboard shortcut opens, closes, and selects in present and comment modes`, `CMH-DECK-06: overview clones stay out of tab order`, `CMH-DECK-05: Export Offline round-trips a deck (mode + comments + card jump) with zero network` |
| CMH-DECK-08 | The shipped showcase deck's triage board opts into widget drag-and-drop, so decision cards can move between columns inside the scaled fixed-stage deck and Copy all reports the layout change like report-mode widget moves. | `tests/62-deck-regressions.spec.js` - `CMH-DECK-08: showcase deck triage cards drag between columns` |
| CMH-DECK-09 | Mermaid diagrams authored on the light parchment deck slides render as visible nodes and edges with high-contrast labels and connectors against the solid slide background instead of washing out. | `tests/62-deck-regressions.spec.js` - `CMH-DECK-09: showcase deck Mermaid diagram renders with readable contrast` |
| CMH-DECK-10 | Deck table header cells use a solid raspberry header background and a high-contrast label color, so authored tables on the light parchment slides keep readable headers regardless of the document theme variables. | `tests/62-deck-regressions.spec.js` - `CMH-DECK-10: showcase deck table headers have readable contrast` |
| CMH-DECK-11 | The deck comment-mode toggle renders the commentable-html brand icon as decorative SVG instead of the wide text label, exposes `Comment Mode` through both `title` and `aria-label`, shows the `Comment Mode` tooltip from the button rather than the icon, preserves the `aria-pressed` toggle state, and still enters and exits comment mode without changing the existing behavior. | `tests/52-deck.spec.js` - `CMH-DECK-11: comment-mode toggle uses the brand icon label and still toggles` |
| CMH-DECK-07 | `dev/tools/check_vendor.py` verifies the vendored frontend-slides subtree against `vendor/frontend-slides/MANIFEST.sha256`: it passes on the pristine tree, fails closed on a changed, unknown, or removed file, fails on a reintroduced denylisted file (`deploy.sh`, `export-pdf.sh`) or a `.claude-plugin/` dir, and `--update` regenerates the manifest after a reviewed resync. | `tests/test_check_vendor.py` - `test_real_vendor_matches_manifest`, `test_changed_file_fails`, `test_unknown_file_fails`, `test_removed_file_fails`, `test_reintroduced_deploy_sh_is_forbidden`, `test_claude_plugin_dir_is_forbidden`, `test_update_regenerates_manifest` |
| CMH-DECK-12 | The shipped `tools/validate/cmhval/contrast.py` utility parses CSS colors (`#rgb`, `#rrggbb`, `rgb()`/`rgba()`, simple `var()` values, and common names), computes the WCAG relative-luminance contrast ratio, and flags pairs below the configurable 4.5:1 default. `deck/deck_validate.py` applies it to the deck CONTENT region for explicit inline `style` pairs, CSS rules whose same declaration block sets `color` plus the last effective `background` or `background-color`, and deck theme variable pairs (`--slide-fg` with `--slide-bg`/`--stage-bg`); errors name the selector, element, or variable pair and both colors so low-contrast authoring defects fail before publish. The parser rejects non-finite and malformed `rgb()`/`rgba()` colors, ignores colors embedded only inside `url(...)` or quoted strings while preserving real fallback colors, and skips semi-transparent backgrounds because their ancestor backdrop is unknown. | `tests/test_contrast.py` - `test_cmh_deck_12_black_white_ratio_is_21_to_1`, `test_cmh_deck_12_equal_colors_ratio_is_1_to_1`, `test_cmh_deck_12_parser_supports_hex_rgb_rgba_and_vars`, `test_cmh_deck_12_parser_rejects_non_finite_rgb_channels`, `test_cmh_deck_12_parser_rejects_malformed_rgb_arity`, `test_cmh_deck_12_finds_low_and_good_pairs`, `test_cmh_deck_12_finds_configured_variable_pairs`, `test_cmh_deck_12_skips_semi_transparent_backgrounds`, `test_cmh_deck_12_background_shorthand_uses_declaration_order`, `test_cmh_deck_12_background_url_text_is_not_a_color`, `test_cmh_deck_12_color_in_quoted_string_content_is_not_a_color`, `test_cmh_deck_12_background_url_fallback_color_is_detected`; `tests/test_deck_validate.py` - `test_cmh_deck_12_low_contrast_css_pair_fails_with_selector`, `test_cmh_deck_12_background_shorthand_overrides_background_color`, `test_cmh_deck_12_low_contrast_theme_variables_fail`, `test_cmh_deck_12_non_finite_rgb_does_not_crash`, `test_cmh_deck_12_semi_transparent_background_is_skipped`, `test_cmh_deck_12_good_contrast_inline_pair_passes` |
| CMH-DECK-13 | Code, KQL, and diff blocks authored on the light parchment deck slides use deck-scoped surfaces, readable body text, syntax token colors that stay distinct inside rendered diffs, and add/delete row tints that keep text at AA contrast regardless of the document theme variables. | `tests/62-deck-regressions.spec.js` - `CMH-DECK-13: showcase deck code, KQL, and diff blocks keep readable contrast` |
| CMH-DECK-SHOWCASE-01 | The shipped live-demo set includes a `kind=slides` showcase deck (`examples/deck-showcase.html`) plus its reusable one-shot authoring prompt (`examples/prompt-showcase.md`). The deck is a light "Parchment and Amber" pitch (pinned `data-theme="light"`) that threads a single community-garden plan as its one running example. The prompt enumerates the plugin features to cover (comment on anything, Chart.js charts, Mermaid diagrams, drag-and-drop triage board, rendered code diffs, syntax-highlighted code and KQL, layered checklist, Copy all, handled-comment pruning, exports, deck navigation, split-screen slide-overview navigator, and strict validator) and prescribes a concrete slide outline. The deck declares `data-cmh-mode="deck"` on the active `#commentRoot`, validates cleanly under `deck/deck_validate.py` (unique stable slide ids, no remote fonts / media / active content, `prefers-reduced-motion` rule), embeds the current runtime version, names itself in `data-doc-source`, and is a pure build artifact of `dev/examples/src/deck-showcase.html` - `build.py --check` catches a hand-edit to its CONTENT region and reports an orphan `examples/deck-*.html` with no source, so a stale or clobbered deck cannot ship. The site demos list syncs the deck under `site/commentable-html/demo/` next to the report demos. | `tests/test_deck_example.py` - `test_deck_example_and_prompt_exist`, `test_showcase_prompt_prescribes_features_and_slide_outline`, `test_deck_declares_slides_kind_and_deck_mode`, `test_deck_validates_clean`, `test_deck_embeds_current_version`, `test_deck_data_doc_source_matches_shipped_filename`, `test_build_check_flags_a_hand_edit_to_the_deck_content`, `test_build_check_flags_an_orphaned_deck_with_no_source` |
| CMH-DECK-SHOWCASE-02 | The shipped showcase deck mounts as a real deck-mode commentable document, starts in present mode with deck controls, toggles to comment mode so a reviewer can create a text comment on a slide, and contains working rich-content examples on the light parchment slides: a commentable chart, rendered Mermaid diagram, rendered diff plus syntax-highlighted KQL/code, drag-and-drop triage board, and initialized layered checklist. | `tests/62-deck-regressions.spec.js` - `CMH-DECK-SHOWCASE-02: showcase deck mounts in deck mode and is commentable` |
| CMH-DECK-SHOWCASE-03 | The showcase deck surfaces the install call to action EARLY, not only on the final slide: an Act 2 slide well before the close carries both agents' exact install commands as code blocks (Copilot and Claude `plugin marketplace add` plus `plugin install commentable-html@urikan-ai-marketplace`) alongside the live-demo, GitHub, and tutorial links, satisfying the install-CTA-early requirement (TASK-41). | `tests/62-deck-regressions.spec.js` - `CMH-DECK-SHOWCASE-03: an early install CTA shows both agents before the final slide` |
| CMH-DECK-SHOWCASE-05 | The showcase deck's narrative is header-first and front-loaded for non-technical viewers: it opens with a dedicated logo-plus-pitch slide, moves the website-style medium comparison ahead of the feature tour, places the shipped example-prompts slide and the two-review-prompts slide early and adjacent, keeps the shipped board/checklist widget defaults unchanged while restructuring the story, removes the redundant "Stop describing edits. Point at them." recap slide, and closes with a "What's next?" link hub followed by a separate "Questions?" slide. | `tests/62-deck-regressions.spec.js` - `CMH-DECK-SHOWCASE-05: showcase deck front-loads the comparison and prompts, keeps widget defaults, and closes with what's next plus questions` |
| CMH-DECK-SHOWCASE-04 | The one-shot showcase-deck authoring prompt (`examples/prompt-showcase.md`) is a pure build artifact of an INDEPENDENT dev-side source (`dev/examples/src/prompt-showcase.md`) that `build.py` copies verbatim, so `build.py --check` reproduces it byte-identically and flags a hand-edit to the shipped copy (mirroring the report/deck example self-source contract). The prompt is content-complete enough to regenerate the deck in one pass: it prescribes the five-act slide outline, pins the Parchment-and-Amber theme tokens (`#f7f4ef`, `#b11f4b`, `#1b1f3b`, the `245, 158, 11` amber highlight, `data-theme="light"`), states the deck constraints (no remote fonts / media / scripts, reduced-motion, stable `data-slide-id`, `commentable-html-kind=slides`), threads one running community-garden example, and gives cwd-safe validate/scaffold commands. Because a live LLM regeneration cannot run in a hermetic test, single-pass reproducibility is verified by the content-completeness checks PLUS proving the deck the prompt targets (`examples/deck-showcase.html`) passes `deck_validate.py --strict` right now. | `tests/test_prompt_showcase.py` - `test_prompt_and_source_exist`, `test_shipped_prompt_is_byte_identical_to_its_source`, `test_prompt_prescribes_the_five_act_slide_outline`, `test_prompt_pins_the_parchment_and_amber_theme_tokens`, `test_prompt_states_the_deck_constraints`, `test_prompt_threads_one_running_community_garden_example`, `test_prompt_gives_cwd_safe_validate_and_scaffold_commands`, `test_shipped_deck_passes_strict_deck_validate`, `test_build_check_reproduces_the_prompt_and_flags_a_hand_edit` |
| CMH-DECK-EXPORT-01 | All four exports work on the shipped showcase DECK (`examples/deck-showcase.html`, a `kind=slides` deck), reached through comment mode. Export Portable produces a self-contained `-portable.html` that preserves `data-cmh-mode="deck"`, the `kind=slides` meta, unique slide ids, and the embedded comment, and reopens with the deck runtime re-activating on slide 1 and the comment restored (the only permitted network is the optional mermaid CDN, never content or a companion file). Export Offline snapshots the deck's rendered Mermaid to inline SVG and its Chart.js-style canvas to an inline data-image, strips all remote loaders, passes BOTH `validate/validate.py --strict` and `deck/deck_validate.py --strict`, and reopens with the network fully blocked (zero external requests, deck re-activates, comment restored). Export Plain strips the commenting layer (no runtime, sidebar, handled-id store, or toolbar) while keeping the slide DOM and theme styling. Export Markdown is a deterministic structural export (slide headings, fenced mermaid, diff, and kusto code blocks, a chart placeholder, and a review-comment section) with no slide markup leaking. | `tests/65-deck-exports.spec.js` - `CMH-DECK-EXPORT-01: Export Portable round-trips the showcase deck and reopens self-contained`, `CMH-DECK-EXPORT-01: Export Offline snapshots the deck mermaid + chart, validates strict, and reopens with zero network`, `CMH-DECK-EXPORT-01: Export Plain strips the layer but keeps the showcase deck slides and styling`, `CMH-DECK-EXPORT-01: Export Markdown produces a deterministic structural export of the showcase deck` |
| CMH-DECK-AUDIT-01 | `dev/tools/audit.mjs` (dev-only) tours any commentable HTML in headless Chromium across viewports and colour schemes, detects deck vs flat, and emits numbered screenshots plus a machine-readable `observations.json` (isDeck, slideCount, console/pageErrors, external requests, overflow, per-step screenshots) and a `report.md` for an AI agent to review; it exits non-zero only on a harness failure (a bad target), not on findings. Overflow detection covers BOTH horizontal overflow and VERTICAL clipping (`clip-y`: content taller than a clipped box, the dominant fixed-stage deck defect). See `dev/AUDIT.md` for the multi-agent review workflow. | `tests/53-audit.spec.js` - `CMH-DECK-AUDIT-01: the audit harness tours a deck and emits screenshots + observations`; `tests/52-deck.spec.js` - `CMH-DECK-05: slides fit the fixed 1080px stage and vertical overflow is detectable as clipping` |
| CMH-TUT-SHOTS-01 | `dev/tools/capture_tutorial.mjs` (dev-only, not shipped) regenerates every tutorial screenshot (the `garden-*.png` images that `docs/TUTORIAL.md` embeds) with one command: with no arguments it drives the shipped community-garden example into `docs/assets` at a fixed 1320x900 (2x) viewport, and it is exposed as `npm run shots`. `npm run shots:check` captures into repo-root `tmp/` and compares against the committed `docs/assets` images without rewriting them, failing on missing or stale screenshots; `npm test` and `python scripts/rebuild_all.py --check` run that drift check so stale committed screenshots are gated. The tool pins the capture clock, random seed, viewport, locale, timezone, motion preferences, browser font rendering flags, CSS animation state, and capture fonts, then normalizes PNG output so all nine screenshots are byte-identical across repeated captures on the same browser environment. Optional positional args `[example] [outDir] [prefix]` capture or check any example (the prefix is basename-sanitized), and `--print-paths` resolves the defaults without launching a browser. See `dev/README.md` ("Regenerating tutorial screenshots"). | `tests/54-tutorial-shots.spec.js` - `one command regenerates and checks all tutorial screenshots, deterministically (CMH-TUT-SHOTS-01)` (checks the no-arg `--print-paths` defaults, runs the tool twice into nested temp dirs, asserts all nine shots exist, verifies `--check` accepts regenerated shots, and proves `--check` fails on a stale PNG); `scripts/test_rebuild_all.py` - `test_check_runs_build_screenshots_and_site_in_order_with_check_flag` |
| CMH-DECK-14 | When asked to plan or design a deck (not just scaffold one), the SKILL.md deck section routes the agent to the vendored frontend-slides design system before scaffolding: it names `vendor/frontend-slides/bold-template-pack/selection-index.json`, `STYLE_PRESETS.md`, `html-template.md`, and `animation-patterns.md`, and directs choosing the slide outline and theme (with safe/bold/wildcard title-slide options) up front, not only at the fill step. | `tests/test_deck_planning_routing.py` - `test_plan_step_names_the_frontend_slides_design_docs`, `test_planning_happens_before_scaffolding` |
| CMH-DECK-15 | Deck comment-mode chrome stays out of the way: the corner comment-mode toggle is hidden at every viewport width while the comment side panel is open (its accent colour reads poorly over a slide, and the panel carries its own header controls), and hiding the panel brings the toggle back so present mode stays reachable; the comments action toolbar (`.cm-toolbar`) never appears in a deck in either mode, leaving only the single corner icon and the nav bar; and when the panel is hidden in comment mode the slide stage spans the full viewport width (the sidebar inset is gated on the panel actually being open) instead of leaving a reserved blank column. | `tests/52-deck.spec.js` - `CMH-DECK-15: the comment-mode toggle hides while the side panel is open (all widths)`, `CMH-DECK-15: the comments action toolbar never appears in a deck`, `CMH-DECK-15: the slide stage spans the full width when the panel is hidden in comment mode` |
| CMH-DECK-16 | The split-screen slide overview is polished for legibility and dismissal: the panel uses a light accent-tinted (red-ish) background, the Close button uses the accent fill with accent-foreground text, and the slide count (`N slides`) shows next to the `Slide overview` title; a click on the main deck area (a slide / the stage / the content root, but not the panel, nav bar, or toggle) closes it; the overview grid scrolls reliably when the slides overflow the viewport height; and overview thumbnails force-reveal animated slide content so each thumbnail previews the slide's final rendered state. | `tests/52-deck.spec.js` - `CMH-DECK-16: the overview is accent-tinted with a red Close button and shows the slide count`, `CMH-DECK-16: clicking the main deck area closes the overview`, `CMH-DECK-16: the overview grid scrolls when the slides overflow the viewport`, `CMH-DECK-16: overview thumbnails force-reveal animated slide content` |
| CMH-DECK-17 | Deck URLs deep-link by stable `data-slide-id`: on init a matching `location.hash` activates that slide, changing slides replaces the current hash without adding browser history entries, and browser `hashchange` events navigate to matching slides while non-deck documents keep the existing heading-anchor behavior. | `tests/52-deck.spec.js` - `CMH-DECK-17: deck URL hash restores and tracks slides without history spam` |

## Coverage gaps

The following features are documented but lack full automated behavior coverage, or have a known mismatch between spec and documentation:

**Remaining manual or design-only gaps:**
- CMH-DECK-01: deck-intent detection and the "confirm before building a real deck" gate are an agent-behavior convention in `SKILL.md` ("Deck capability"), not an automated test.
- CMH-DECK-03b: the "prefer the Anthropic `pptx` skill when installed, fall back to the local extractor" preference is prompt-level wording in `SKILL.md`; only the deterministic escaping and the local fallback (CMH-DECK-03a) are automated.
- CMH-CONTENT-05: ADO resource links in authored content are a manual authoring convention with no validator or E2E enforcement.
- CMH-CONTENT-06: prose cross-reference links are only partially covered by TOC anchor tests.
- CMH-KQL-01: a framed KQL figure (`figure.cmh-kql`) is now hard-enforced to carry a Run in Azure Data Explorer link (CMH-KQL-07, a validator error). Bare, unframed `<pre>` KQL blocks are now also enforced (CMH-KQL-08): they must be marked `data-cmh-kql-no-cluster` when there is genuinely no cluster to run them on, so a missing cluster is a deliberate choice, not an accidental omission.
- CMH-CHART-05: chart recipe semantics, dark chart palette choices, and data hygiene rules are documentation and review conventions, not automated tests.
- CMH-WIDGET-18: widget drag suppresses text selection inside draggable cards by design; drag behavior is covered, but the selection-suppression decision itself is a manual UX contract.
- CMH-NET-01 / CMH-MMDLOAD-01: CDN opt-in and local vendoring choices are manual. CMH-MMDLOAD-01 now has E2E coverage for the default CDN path; manual guidance for vendor/self-host advice remains.

**Documentation/implementation mismatches (spec is correct; docs or tooling do not reflect it):**
- CMH-COPY-06: `references/limitations.md` implies all copy paths have the `prompt` fallback; only Copy all does. The per-block no-prompt rule now has E2E coverage, but the docs still need alignment outside this tests-only pass.
- CMH-SEC-01: the "callers must sanitize untrusted host HTML before wrapping" rule is stated in the spec and is now documented in `SKILL.md` (the "Trust boundary (MUST)" note) and `new_document.py --help`; it still has no automated enforcement in the validator or tooling.
