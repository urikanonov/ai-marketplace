name: plugin-tests

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      plugins: ${{ steps.find.outputs.plugins }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Discover plugin test suites
        id: find
        shell: bash
        run: |
          set -euo pipefail
          # A plugin has a Node/Playwright suite when plugins/<plugin>/dev/package.json exists.
          plugins=$(find plugins -mindepth 3 -maxdepth 3 -type f -path 'plugins/*/dev/package.json' \
            -printf '%h\n' | sed 's:/dev$::' | sort -u \
            | jq -R -s -c 'split("\n") | map(select(length > 0 and test("^plugins/[A-Za-z0-9._-]+$")))')
          {
            echo 'plugins<<__EOF__'
            echo "$plugins"
            echo '__EOF__'
          } >> "$GITHUB_OUTPUT"
          echo "Discovered plugin test suites: $plugins"

  playwright:
    needs: discover
    if: ${{ needs.discover.outputs.plugins != '[]' && needs.discover.outputs.plugins != '' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        plugin: ${{ fromJSON(needs.discover.outputs.plugins) }}
    defaults:
      run:
        working-directory: ${{ matrix.plugin }}/dev
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: ${{ matrix.plugin }}/dev/package-lock.json
      - name: Verify committed lockfile
        run: test -f package-lock.json || { echo "::error::this plugin's dev/ needs a committed package-lock.json for npm ci"; exit 1; }
      - name: Install dependencies (no lifecycle scripts)
        run: npm ci --ignore-scripts
      - name: Verify committed test fixtures
        if: matrix.plugin == 'plugins/commentable-html'
        working-directory: .
        run: node plugins/commentable-html/dev/tests/fixtures/generate.mjs --check
      - name: Resolve Playwright cache key inputs
        id: pw
        shell: bash
        run: |
          set -euo pipefail
          node -e 'process.stdout.write("version=" + require("@playwright/test/package.json").version + "\n")' >> "$GITHUB_OUTPUT"
          echo "imageos=${ImageOS:-$RUNNER_OS}" >> "$GITHUB_OUTPUT"
      - name: Restore Playwright browser cache
        id: pwcache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ steps.pw.outputs.imageos }}-${{ runner.arch }}-chromium-${{ steps.pw.outputs.version }}
      - name: Install Playwright browser and system deps
        run: npx playwright install --with-deps chromium
      - name: Save Playwright browser cache
        if: steps.pwcache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ steps.pw.outputs.imageos }}-${{ runner.arch }}-chromium-${{ steps.pw.outputs.version }}
      - name: Run Playwright tests
        run: npm test

  python:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"
      - name: Run plugin Python test suites
        shell: bash
        run: |
          set -euo pipefail
          found=0
          for tests in plugins/*/dev/tests; do
            [ -d "$tests" ] || continue
            if find "$tests" -maxdepth 1 -type f -name 'test_*.py' -print -quit | grep -q .; then
              found=1
              python -m unittest discover -s "$tests" -p "test_*.py"
            fi
          done
          if [ "$found" -ne 1 ]; then
            echo "::error::no plugin Python test suites were discovered"
            exit 1
          fi

  plugin-tests:
    needs: [discover, playwright, python]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Gate on plugin test results
        env:
          DISCOVER_RESULT: ${{ needs.discover.result }}
          PLAYWRIGHT_RESULT: ${{ needs.playwright.result }}
          PYTHON_RESULT: ${{ needs.python.result }}
          DISCOVERED: ${{ needs.discover.outputs.plugins }}
        run: |
          echo "discover=$DISCOVER_RESULT playwright=$PLAYWRIGHT_RESULT python=$PYTHON_RESULT plugins=$DISCOVERED"
          if [ "$DISCOVER_RESULT" != "success" ]; then
            echo "::error::plugin discovery failed"
            exit 1
          fi
          if [ -z "$DISCOVERED" ] || [ "$DISCOVERED" = "[]" ]; then
            echo "::error::no plugin test suites were discovered; expected at least one plugins/*/dev/package.json"
            exit 1
          fi
          if [ "$PLAYWRIGHT_RESULT" = "failure" ] || [ "$PLAYWRIGHT_RESULT" = "cancelled" ]; then
            echo "::error::plugin Playwright tests did not pass"
            exit 1
          fi
          if [ "$PYTHON_RESULT" != "success" ]; then
            echo "::error::plugin Python tests did not pass"
            exit 1
          fi
