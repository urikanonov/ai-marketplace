# Adding a feature

This checklist now lives as the always-available `adding-a-feature` project skill so agents auto-load
it when implementing or reviewing a feature. See
[.github/skills/adding-a-feature/SKILL.md](../.github/skills/adding-a-feature/SKILL.md) for the full
end-to-end workflow (issue-first + worktree, test-first TDD and the spec-row discipline, editing the
owning split partial and wiring the shared layers, the scaffold + apply tool pattern, the coverage gates,
adding a demo and running the E2E audit, and the version / changelog / rebuild / validate steps).
