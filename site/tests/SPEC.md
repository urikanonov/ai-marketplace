# Site feature specification

The GitHub Pages site under `site/` is a fully static marketing and documentation surface for the
marketplace. It has a hub page (`site/index.html`), one generated plugin page per published plugin
(`site/commentable-html/index.html` and `site/urikan-ai-marketplace-auto-updater/index.html`), and a
generated tutorial page
(`site/commentable-html/tutorial/index.html`). The client-side script (`site/src/site.js`) is
progressive enhancement only: it wires up the copy buttons, the footer year, the demo slider, and
the tutorial image lightbox, and never fetches or injects remote data.

The core invariant is static self-containment. Every dynamic-looking region (the plugins grid, the
version badge, the changelog, the tutorial body, the synced demos and tutorial images) is generated
at build time by `scripts/build_site_data.py` from the repository's own sources
(`.github/plugin/marketplace.json` and the plugin `CHANGELOG.md` / `TUTORIAL.md`) and written into
marker regions of the committed HTML. The published site never fetches at runtime (no CORS or
`file://` breakage, no GitHub API rate limits, no runtime DOM injection), all text is HTML-escaped,
and every URL is allowlisted before it is written. The one deliberate remote dependency is the hub's
GitHub star widget, which is CSP-scoped and degrades to a plain link when blocked.

Coverage notation: each row lists the strongest automated coverage I found. `manual` means the
behavior is a documented convention rather than an automated test. Browser (E2E) rows cite
`site/tests/tests/site-*.spec.js`; generator rows cite `scripts/test_build_site_data_*.py`. The site is
gated on `main` by the required `site` check, which runs the generator unit tests and the browser
suite (see `.github/workflows/pages.yml`).

## Build pipeline: generation, escaping, and drift

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-BUILD-01 | The plugins grid, the version badge, and the changelog are generated from `marketplace.json` and the plugin `CHANGELOG.md` and written into named marker regions (`BEGIN:...`/`END:...`); the site performs no client-side fetch. | `scripts/test_build_site_data_render.py` - `RenderPluginsTests`, `ReplaceRegionTests`, `ChangelogInlineTests` |
| SITE-BUILD-02 | All repository text is HTML-escaped and every URL is passed through `safe_url`, which allows only https, mailto, and in-repo relative URLs and neutralizes dangerous or off-site schemes, control-char scheme smuggling, and backslash protocol-relative hosts, so repository content can never inject markup or an active link. | `scripts/test_build_site_data_core.py` - `SafeUrlTests`, `RenderPluginsTests.test_escapes_text_and_neutralizes_bad_homepage`, `RenderMarkdownTests.test_xss_neutralized_in_alt_and_raw_html`, `RenderMarkdownTests.test_dangerous_link_scheme_neutralized` |
| SITE-BUILD-03 | The changelog parser filters the root changelog to the target plugin, keeps all bullets for a per-plugin changelog, does not drop ungrouped bullets, joins continuation lines, strips the leading plugin name and version from an entry, and renders each bullet as inline Markdown that preserves code spans. | `scripts/test_build_site_data_core.py` - `ParseChangelogTests`, `CleanEntryTests`, `ChangelogCandidatesTests`, `ChangelogInlineTests`, `MdInlineTests` |
| SITE-BUILD-04 | The tutorial page is rendered from the skill's `docs/TUTORIAL.md`: headings offset, lists, bold, code spans and fenced blocks, images, and links render safely, and skill-root-relative example links are rewritten to the live demo report under `../demo/` that the site hosts. The tutorial text must use the live toolbar button label "Comments" (not the stale "Show") to open the comments panel, so UI label drift is caught before it ships. | `scripts/test_build_site_data_core.py` - `RenderMarkdownTests`, `RenderMarkdownOrderingTests`, `SiteTutorialMarkdownTests.test_rewrites_local_example_links_to_demo`, `SiteTutorialMarkdownTests.test_toolbar_open_button_label_is_comments_not_show` |
| SITE-BUILD-05 | The demo reports under `site/commentable-html/demo/` are synced from the skill's `examples/`; a content difference or a missing destination is flagged under `--check` and fixed on a full run, and an orphaned demo is removed. Orphans are discovered and reported in deterministic sorted order so `--check` output does not depend on directory-listing order. | `scripts/test_build_site_data_sync.py` - `SyncDemosDriftTests`, `SyncOrphanTests` (including `test_orphans_reported_in_sorted_order`) |
| SITE-BUILD-06 | Tutorial images are synced from the skill's `docs/assets/`; a content difference is flagged then synced, an orphaned image whose source is gone is removed, and a missing source directory orphans the committed images. | `scripts/test_build_site_data_sync.py` - `SyncTutorialImagesTests` |
| SITE-BUILD-07 | Every committed HTML page references its CSS and JS with a content-hash cache-busting query stamp at every path prefix (`assets/`, `./assets/`, `../assets/`); stamping replaces a stale stamp, is idempotent, leaves non-asset URLs untouched, and the committed pages carry current stamps with no stale or unstamped asset ref. | `scripts/test_build_site_data_sync.py` - `StampAssetsTests`, `StampWiringTests` |
| SITE-BUILD-08 | `build_site_data.py --check` exits non-zero if the committed `site/` is stale versus its sources: a stale generated region, a drifted demo or tutorial image, or a stale asset stamp all fail the check (the required `site` check runs it on every pull request). | `scripts/test_build_site_data_drift.py` - `CheckDriftTests`, `SyncDemosDriftTests`, `SyncTutorialImagesTests`, `StampWiringTests` |
| SITE-BUILD-09 | `replace_region` replaces exactly one delimited region and refuses to run when a marker region is missing or duplicated, so a generation step can never silently no-op or double-write. | `scripts/test_build_site_data_core.py` - `ReplaceRegionTests` |
| SITE-BUILD-10 | Changelog plugin matching is anchored and case-insensitive so a plugin name matches its own heading but not a mid-text mention or another plugin. | `scripts/test_build_site_data_core.py` - `MentionsPluginTests` |
| SITE-BUILD-11 | The changelog shows the two most recent releases inline and folds the next five into a collapsed `<details>`; any releases beyond those seven are not rendered inline but are summarized with a link to the full changelog in source, so the page never grows without bound. | `scripts/test_build_site_data_render.py` - `ChangelogInlineTests.test_older_releases_capped_and_rest_linked_to_source` |
| SITE-BUILD-12 | Each plugin card renders its manifest `category` as a Title Case badge (via the `category_label` display mapping - `commentable-html` shows "Planning and Analysis", not the lowercase slug) and its `keywords` as chips. | `scripts/test_build_site_data_render.py` - `RenderPluginsTests.test_real_manifest_commentable_badge_and_chips` |
| SITE-BUILD-13 | The served `site/dist/assets/styles.css` is assembled by `build_site_data.py` from the `NN-topic.css` partials under `site/css/`, discovered by DIRECTORY SORT (no hand-maintained order list, so adding a partial does not edit the generator and two PRs adding partials do not collide there); the numeric-prefix sort is the cascade order. The committed stylesheet is byte-identical to the concatenation and `--check` fails if it drifts from the partials; a stray `.css` file that is not a `NN-topic.css` partial is rejected with a clear `SystemExit`. | `scripts/test_build_site_data_sync.py` - `StylesConcatTests.test_concat_matches_committed_stylesheet`, `StylesConcatTests.test_parts_exist_and_base_loads_first`, `StylesConcatTests.test_a_stray_non_numbered_css_file_is_rejected` |
| SITE-BUILD-14 | The hub, plugin, and tutorial pages are pure build artifacts: their hand-edited SOURCE lives under `site/pages/` and `build_site_data.py` assembles the committed `site/dist/**` pages from it, injecting a `GENERATED FILE - DO NOT EDIT` banner (and a `/* ... */` banner in `styles.css`) that names the source. Because the source is independent of the artifact, `--check` compares the whole built page (not just the marker regions) to the committed file, so a hand-edit to any part of a built page (hub, plugin, or tutorial) - or a built page missing entirely - fails `--check`. The hub and plugin pages are REQUIRED: a missing source raises a clear `SystemExit` (they cannot be dropped). The tutorial page is OPTIONAL: removing its source removes the page (its built artifact is deleted on a normal build and its `llms.txt` and `sitemap.xml` links are dropped), and a built tutorial artifact left behind after its source was removed is flagged as an orphan by `--check`. The `styles.css` cache-bust stamp on each page is derived from the source partials (not the on-disk stylesheet), so a missing or stale `site/dist/assets/styles.css` is reported as drift rather than crashing `--check`. A build on a fresh checkout with no `site/dist/` tree yet creates the nested page directories instead of crashing. `build_page` fails loudly (`SystemExit`) on a source with no doctype, more than one LINE-LEADING doctype (a merge artifact - a literal `<!doctype` embedded in prose or a script string does not count), or an unknown region-filler kind, and tolerates a leading UTF-8 BOM (never emitting it into the artifact); `build_styles` likewise strips a BOM from any CSS partial and errors clearly on a missing css directory. The editable sources under `site/pages/` never carry the generated banner. | `scripts/test_build_site_data_drift.py` - `CheckDriftTests.test_check_flags_a_hand_edited_built_page`, `test_check_flags_a_hand_edited_built_hub_page`, `test_check_flags_an_orphaned_page_whose_source_was_removed`, `test_check_flags_a_missing_built_page_when_its_source_exists`, `test_check_flags_a_missing_built_hub_page`, `test_check_flags_a_missing_built_stylesheet_without_crashing`, `test_styles_asset_hash_ignores_a_stale_on_disk_stylesheet`, `test_build_writes_nested_artifacts_on_a_fresh_checkout_without_site_tree`, `test_write_removes_an_orphaned_page_whose_source_was_removed`, `test_removing_tutorial_source_drops_its_llms_link`, `test_removing_tutorial_source_drops_its_sitemap_entry`, `test_missing_required_page_source_errors_clearly`, `test_missing_required_plugin_page_source_errors_clearly` (end-to-end via `main(["--check"])`); `PageBannerAndGuardTests` (banner present/idempotent, `build_page` fills+banners+rejects unknown kind, no-doctype and two-doctype sources+tolerates a BOM and a literal in-content doctype, `build_styles` strips a partial BOM and errors on a missing css directory, hand-edit differs from a fresh build, missing artifact is drift, committed sources are banner-free, stylesheet carries the banner) |
| SITE-BUILD-15 | The generator fails loudly with a clear `SystemExit` (never a raw traceback) on a missing or malformed required input, or a malformed output tree: a missing or non-JSON marketplace manifest, a missing committed cache-busted asset (`site/dist/assets/site.js`), a missing `site/css/` directory, or a `write_text`/sync target whose parent path is a regular file (a directory-vs-file conflict). Existence checks use `os.path.isfile` and reads catch `OSError`, so a path that is a directory where a file is expected also fails clearly rather than crashing `open`. Each message names the offending path and how to fix it. | `scripts/test_build_site_data_drift.py` - `CheckDriftTests.test_missing_manifest_errors_clearly`, `test_malformed_manifest_errors_clearly`, `test_missing_required_site_js_asset_errors_clearly`; `PageBannerAndGuardTests.test_write_text_errors_clearly_when_a_parent_is_a_file`, `test_write_text_creates_missing_parent_dirs`, `test_build_styles_errors_on_a_missing_css_directory`, `test_build_styles_errors_on_an_empty_css_directory` |
| SITE-BUILD-16 | The generated `site/dist` is byte-reproducible across OS/Python so the required `site` gate (which the `build_site_data.py` wrapper enforces by `sys.exit`-ing the `--check` return code) cannot fail merely because a commit was built off-Linux. Every text output is written LF-normalized, and the Claude Desktop skill ZIP - the one output assembled from raw working-tree file bytes - normalizes each text member's line endings to LF (folding CR/CRLF), leaving binary members (detected by a NUL byte) byte-for-byte, so a CRLF checkout (for example a Windows one where `.gitattributes eol=lf` is not honored) produces the same ZIP bytes as a Linux CI rebuild. The deterministic ZIP container (fixed 1980 mtimes, fixed permissions/creator, sorted members) plus the CLI wrapper propagating the `--check` exit code together mean stale committed site data fails CI while a clean tree exits 0. | `scripts/test_build_site_data_sync.py` - `SkillZipTests.test_text_members_are_line_ending_normalized_for_cross_platform_determinism`, `test_zip_bytes_identical_from_crlf_and_lf_checkouts`, `test_write_is_idempotent_and_avoids_churn`; `scripts/test_build_site_data_drift.py` - `CheckDriftTests.test_cli_wrapper_propagates_nonzero_exit_on_drift` |

## Hub page

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-HUB-01 | The hub renders the marketplace title, the brand logo, at least two plugin cards including `commentable-html`, and the one-time `marketplace add` install command. | `site/tests/tests/site-hub.spec.js` - `hub renders with plugins, install command, and logo` |
| SITE-HUB-02 | The crimson theme is applied: the `--cp-accent` custom property resolves to `#b11f4b`. | `site/tests/tests/site-plugin.spec.js` - `theme variables are present (light + crimson)` |
| SITE-HUB-03 | The hub embeds the GitHub star widget and its Content-Security-Policy permits exactly what the widget needs: the `buttons.github.io` script/frame host, the `api.github.com` connect host for the star count, and `'unsafe-inline'` in `style-src` for the style the widget injects. | `site/tests/tests/site-install.spec.js` - `hub embeds the GitHub star widget and its CSP permits it` |
| SITE-HUB-04 | The star widget degrades to a visible plain link to the repository when its script is blocked. | `site/tests/tests/site-install.spec.js` - `star widget degrades to a visible plain link when its script is blocked` |
| SITE-HUB-05 | The footer year is filled in at load with the current calendar year. | `site/tests/tests/site-plugin.spec.js` - `footer year is filled in with the current year` |
| SITE-HUB-06 | A plugin card is a whole-card click target to its plugin page via the title link and progressive card handler, while the install/copy block and the foot buttons stay independently clickable above it (no navigation, no cursor flicker). The card body carries a pointer cursor so it reads as clickable, while the install/copy block stays a text surface (not pointer). Both published plugins now have a generated page, so both cards navigate; the linkless-card rendering path (a plugin with no generated page has no title link and no Learn more, so its card does not navigate) is covered at the generator level. | `site/tests/tests/site-hub.spec.js` - `a plugin card is clickable across its body, navigating to the plugin page (SITE-HUB-06)`, `the card copy button and Learn more stay independently clickable over the card link (SITE-HUB-06)`, `the plugin card body shows a pointer cursor so it reads as clickable (SITE-HUB-06)`, `the auto-updater card navigates to its plugin page (SITE-HUB-06)`; `scripts/test_build_site_data_render.py` - `RenderPluginsTests.test_card_without_page_has_no_learn_more` |
| SITE-HUB-07 | The plugin card `Learn more` button uses the brand accent color family (its label and border are `--cp-accent`) but renders as a soft/outlined button on a soft accent tint, NOT a solid accent fill, so it is visually distinct from the solid accent-filled active install-agent tab below it (and not the former amber). | `site/tests/tests/site-hub.spec.js` - `the hub Learn more button is accent-colored but visually distinct from the solid install tab (SITE-HUB-07)` |
| SITE-HUB-08 | Selecting text in a plugin-card description or keyword suppresses stretched-card navigation so users can copy the text, while a plain body click with no selection still navigates to the plugin page. | `site/tests/tests/site-links-seo.spec.js` - `plugin card text can be selected without navigation and plain body clicks still navigate (SITE-HUB-08)` |
| SITE-HUB-09 | The hub plugins grid lays each plugin out as a full-width row (the `#plugins .grid` is a single column). The card info is a flat vertical stack - title and version, then the flowing description (capped to a comfortable measure, `max-width: 84ch`), then the tags, then the Learn more/Source actions as a plain left-aligned `.foot` row directly under the tags (no `.body-row`/`.body-main` wrapper and no boxed column to the right of the description, so there is no dead whitespace gap between the prose and the buttons), then the install section - each below the previous and sharing the left edge. | `site/tests/tests/site-hub.spec.js` - `the hub lays each plugin out as a full-width row with the actions in a plain row under the tags (SITE-HUB-09)`; `scripts/test_build_site_data_render.py` - `RenderPluginsTests.test_actions_render_in_a_plain_row_under_the_tags`, `RenderPluginsTests.test_source_only_card_renders_a_lone_source_action` |
| SITE-HUB-10 | The hub plugin cards are ordered commentable-html, then multi-duck, then urikan-ai-marketplace-auto-updater (driven by the marketplace manifest order), so the two flagship skills lead and the infrastructure updater trails. | `site/tests/tests/site-hub.spec.js` - `the hub plugin cards are ordered commentable-html, multi-duck, then the auto-updater (SITE-HUB-10)`; `scripts/test_build_site_data_render.py` - `HubPluginOrderTests.test_render_plugins_orders_cmh_then_multiduck_then_updater` |
| SITE-HUB-11 | Each hub plugin card carries a stable, slugged anchor id `plugin-<name>` and a `scroll-margin-top` so the nav "Plugins" dropdown and the hero pills can scroll straight to a specific plugin's card without it hiding under the sticky navbar. | `site/tests/tests/site-hub.spec.js` - `each hub plugin card has a stable anchor id and clears the sticky nav (SITE-HUB-11)`; `scripts/test_build_site_data_render.py` - `RenderPluginsTests.test_card_has_a_stable_slugged_anchor_id` |
| SITE-HUB-12 | The hub hero shows a row of plugin pills under the CTAs (`.hero-pills`), one per published plugin (friendly label), each a link to that plugin's card anchor (`#plugin-<name>`); clicking a pill scrolls the card into view. The generator emits one pill per manifest plugin in order and an empty string when there are no plugins. | `site/tests/tests/site-hub.spec.js` - `the hero shows a pill per plugin that scrolls to its card (SITE-HUB-12)`; `scripts/test_build_site_data_render.py` - `RenderHeroPillsTests` |
| SITE-HUB-13 | On each hub plugin card a horizontal rule with added spacing separates the Learn more/Source actions from the install-agent tabs: `#plugins .plugin-card .install` carries a top border plus extra top margin and padding, while the `.foot` actions row has no such border, so the two button groups do not read as one. | `site/tests/tests/site-hub.spec.js` - `a horizontal divider with spacing separates the card actions from the install tabs (SITE-HUB-13)` |
| SITE-HUB-14 | Each hub plugin card title shows the plugin's friendly display name from `PLUGIN_DISPLAY_NAMES` (Commentable HTML, Multi Duck, Auto-updater), while the title link `href` and the card anchor id keep the code-name slug. | `site/tests/tests/site-hub.spec.js` - `the hub plugin card titles show the friendly display name while the link href and anchor id keep the code-name slug (SITE-HUB-14)`; `scripts/test_build_site_data_render.py` - `RenderPluginsTests.test_card_title_uses_display_name` |

## Install command block (dual-agent)

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-INSTALL-01 | The install command block is a tabbed UI with one tab per supported agent (GitHub Copilot first and selected by default, Claude Code second). Clicking a tab reveals that agent's commands and hides the other's, keeping the `role="tablist"`/`tab`/`tabpanel` and `aria-selected` wiring in sync; the hub top install shows the marketplace-add command per agent. Tab ids are unique per block so multiple blocks on a page do not collide. | `site/tests/tests/site-install.spec.js` - `the hub install block tabs between Copilot and Claude commands (SITE-INSTALL-01)`; `scripts/test_build_site_data_render.py` - `RenderInstallTests.test_dual_agent_tabs_with_split_rows_and_per_row_copy`, `test_marketplace_only_block_omits_the_plugin_row_but_keeps_both_tabs`, `test_ids_are_unique_per_block_for_aria_wiring` |
| SITE-INSTALL-02 | On a plugin's install block (hub card and plugin page) the marketplace-add and plugin-install commands are on separate, step-numbered rows, each with its own copy button that copies exactly that command; each agent tab carries both rows using that agent's CLI binary (`copilot`/`claude`). | `site/tests/tests/site-install.spec.js` - `the commentable-html install splits marketplace and plugin into copyable rows per agent (SITE-INSTALL-02)`; `scripts/test_build_site_data_render.py` - `RenderInstallTests.test_dual_agent_tabs_with_split_rows_and_per_row_copy`, `RenderPluginsTests.test_card_install_shows_claude_tab_only_for_claude_plugins` |
| SITE-INSTALL-03 | The install tablist supports roving-tabindex keyboard navigation: Arrow keys move selection between agent tabs and focus the newly selected tab. | `site/tests/tests/site-install.spec.js` - `install tabs support arrow-key navigation (SITE-INSTALL-03)` |
| SITE-INSTALL-04 | The Claude tab is offered only for plugins listed in the repo-root `.claude-plugin/marketplace.json` (read by `claude_plugin_names`, which fails closed to an empty set on a missing/malformed mirror); a plugin absent from that mirror renders a tab-less `.install-solo` block with only the Copilot commands and no `claude plugin` command (the generator tests pin this carve-out with a synthetic Copilot-only plugin - both currently shipped plugins are dual-agent). Ids derived from the plugin name are slugged so a name cannot inject into an id/ARIA attribute. | `scripts/test_build_site_data_render.py` - `RenderInstallTests.test_copilot_only_plugin_has_no_claude_tab_or_command`, `RenderInstallTests.test_idbase_is_slugged_so_a_name_cannot_inject_into_id_attributes`, `RenderPluginsTests.test_card_install_shows_claude_tab_only_for_claude_plugins`, `ClaudePluginNamesTests` |
| SITE-INSTALL-05 | A plugin that offers an importable skill (listed in `DESKTOP_SKILLS`; commentable-html and multi-duck) gains a third "Claude Desktop" install tab (hub card and plugin page) whose panel is a ZIP download link plus short import steps (Settings > Features), not a CLI command. The auto-updater is intentionally excluded (its value is a session hook a Desktop skill import cannot provide), so it shows CLI tabs only; the generic hub-hero marketplace-only block never shows the Desktop tab. | `site/tests/tests/site-install.spec.js` - `a skill plugin offers a Claude Desktop ZIP-download tab; the auto-updater does not (SITE-INSTALL-05)`; `scripts/test_build_site_data_render.py` - `RenderInstallTests.test_claude_desktop_tab_offers_a_zip_download_when_desktop_zip_set`, `test_no_claude_desktop_tab_when_desktop_zip_is_absent`, `test_marketplace_only_block_never_shows_a_desktop_tab` |
| SITE-INSTALL-06 | The site build generates a downloadable ZIP of each `DESKTOP_SKILLS` skill (commentable-html and multi-duck) at `site/dist/skills/<skill>.zip`, packaging the git-tracked skill files (exactly what `plugin install` ships, so untracked noise like `.DS_Store`/`__pycache__` never leaks in) under a single top-level `<skill>/` folder (SKILL.md at its root, else the build errors) as Claude Desktop skill import expects. The ZIP is served for download and gated by `build_site_data.py --check` (compared by logical member contents, so a stale/missing/corrupt ZIP fails CI while zlib/platform differences do not). | `site/tests/tests/site-install.spec.js` - `the Claude Desktop skill ZIP is served for download (SITE-INSTALL-06)`; `scripts/test_build_site_data_sync.py` - `SkillZipTests` |
| SITE-INSTALL-07 | The install tablist is a segmented control (a bordered tray of pill buttons) so the tabs read unmistakably as clickable controls: the active tab has a filled, non-transparent background distinct from an inactive tab, and the tablist carries a visible border. | `site/tests/tests/site-install.spec.js` - `the install tabs read as a clickable segmented control (SITE-INSTALL-07)` |
| SITE-INSTALL-08 | On the auto-updater and multi-duck plugin pages the Why (rationale) section leads right after the hero and the Install section sits BELOW it, so a reader gets the rationale before the setup steps. | `site/tests/tests/site-updater-tutorial.spec.js` - `the auto-updater and multi-duck pages lead with the Why section and place Install below it (SITE-INSTALL-08)` |
| SITE-DUAL-01 | The site presents the marketplace as working with BOTH Claude Code and the GitHub Copilot CLI (not Copilot-only): the plugin JSON-LD `operatingSystem` names both agents FOR A CLAUDE-INSTALLABLE PLUGIN and stays Copilot-only for a Copilot-only one, `llms.txt` states the dual support and labels each plugin's agents, and the hub hero + plugin install section state the skills are invokable from each agent's CLI and Desktop app. | `scripts/test_build_site_data_render.py` - `JsonLdTests.test_operating_system_names_both_agents`, `LlmsTests.test_states_both_agents_and_their_install_commands`; `site/tests/tests/site-install.spec.js` - `the pages state dual-agent invocation from each agent's CLI and Desktop app (SITE-DUAL-01)` |

## Plugin page

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-PLUGIN-01 | The plugin page renders the plugin title, a semver version badge, at least four feature blocks, at least one changelog release, and the live demo iframe pointing at the taxi report. | `site/tests/tests/site-plugin.spec.js` - `plugin page renders version, features, changelog, and demo` |
| SITE-PLUGIN-02 | The plugin page keeps a tight CSP: `script-src 'self'`, no star-widget script host, and no `'unsafe-inline'`. | `site/tests/tests/site-install.spec.js` - `plugin and tutorial pages keep a tight CSP (no widget relaxations)` |
| SITE-PLUGIN-03 | The plugin page links to its tutorial page. | `site/tests/tests/site-demo-accessibility.spec.js` - `plugin page links to the tutorial` |
| SITE-PLUGIN-04 | The demo report mounts inside the plugin-page iframe (the CSP permits the same-origin frame and its scripts). | `site/tests/tests/site-demo-accessibility.spec.js` - `demo mounts inside the iframe on the plugin page (CSP allows it)` |
| SITE-PLUGIN-05 | The plugin page hero shows the plugin logo image with descriptive alt text. | `site/tests/tests/site-demo-accessibility.spec.js` - `commentable-html hero shows the plugin logo` |
| SITE-PLUGIN-06 | The plugin-page nav brand link stays on the plugin page (does not jump to the hub), and a Marketplace link sits immediately after the GitHub link and returns to the hub. | `site/tests/tests/site-demo-accessibility.spec.js` - `commentable-html nav keeps the user on the page and offers a Marketplace link` |
| SITE-PLUGIN-07 | The plugin page footer links to the contributing guide, the feature-request form, the issue chooser, the plugin source tree, and the author's LinkedIn profile. | `site/tests/tests/site-updater-tutorial.spec.js` - `the plugin page footer links to contribute, feature request, issues, source, and the author's LinkedIn` |
| SITE-PLUGIN-08 | The plugin page hero leads with the H1 and CTAs; the identity line (logo, name, and version badge) sits below the call-to-action buttons and links to the plugin's source directory on GitHub. | `site/tests/tests/site-demo-accessibility.spec.js` - `the plugin page identity line (logo, name, version) sits below the call-to-action buttons` |
| SITE-PLUGIN-09 | The portability section presents three modes (Non-portable, Portable, Offline), each with a source graph (`.mode-sources`) of color-coded chips showing where every part comes from; Offline shows the mermaid/charts inlined (no CDN chip) while Non-portable keeps a CDN chip. | `site/tests/tests/site-plugin.spec.js` - `the portability section shows three modes including Offline with a source graph (SITE-PLUGIN-09)` |
| SITE-PLUGIN-10 | The portability section carries a footnote (`.modes-note`) explaining that the CDN chip is a network dependency: a Non-portable or Portable report that uses mermaid diagrams or charts needs an internet connection to render them, and Export Offline inlines them to remove that dependency. | `site/tests/tests/site-plugin.spec.js` - `the portability section explains the CDN chip needs a network connection and Offline removes it (SITE-PLUGIN-10)` |
| SITE-PLUGIN-11 | The "What you get" section covers exporting an Offline, zero-network copy (mermaid/chart snapshots inlined, remote loaders stripped) alongside Portable. | `site/tests/tests/site-plugin.spec.js` - `the What you get section covers exporting an Offline, zero-network copy (SITE-PLUGIN-11)` |
| SITE-PLUGIN-12 | The "What you get" section showcases the commentable-decks capability (a fixed 16:9 slide deck that is also a commentable document: present mode, commenting on live slides, slide navigation, Offline handoff), so the shipped deck feature is visible on the plugin page. | `site/tests/tests/site-plugin.spec.js` - `the What you get section showcases the commentable-decks capability (SITE-PLUGIN-12)` |
| SITE-PLUGIN-13 | The plugin page credits the vendored deck engine: a dedicated Credits section above the changelog, and a credit line inside the Commentable decks feature card, both name the upstream `frontend-slides` project and its author Zara Zhang, note the MIT license, and link the upstream repository (`https://github.com/zarazhangrui/frontend-slides`). | `site/tests/tests/site-plugin.spec.js` - `the plugin page credits the vendored frontend-slides deck engine (SITE-PLUGIN-13)` |
| SITE-PLUGIN-14 | The plugin page has a "Private by design" section (`#privacy`, linked from the nav) that emphasizes privacy and compliance: the document and every comment stay local - in the browser's `localStorage` or embedded in the user's own HTML file - are never uploaded or sent to any external service, and there is no server, account, or telemetry; only the Mermaid/Chart.js rendering libraries load from a CDN (not user data) and Export Offline strips even that for air-gapped, sensitive, or regulated use. | `site/tests/tests/site-plugin.spec.js` - `the plugin page has a Private by design section emphasizing local-only data (SITE-PLUGIN-14)` |
| SITE-PLUGIN-14b | The Private by design section lays its four cards out 2x2 on a desktop-width viewport (two columns, no orphan third-row card), collapsing to one column on a narrow viewport. | `site/tests/tests/site-plugin.spec.js` - `the plugin page has a Private by design section emphasizing local-only data (SITE-PLUGIN-14)` |
| SITE-PLUGIN-15 | Every feature card in the "What you get" and "Private by design" sections leads with an inline SVG icon (`.feature-icon`, accent-colored, no external request) placed BESIDE the title on the same row (not stacked on a row of its own above it), so the features read visually rather than as a wall of text. | `site/tests/tests/site-plugin.spec.js` - `each feature card leads with an inline SVG icon beside the title (SITE-PLUGIN-15)` |
| SITE-PLUGIN-16 | The "What you get" section includes a "Rich content" card showcasing the advanced content types beyond prose: Chart.js charts, Mermaid diagrams, drag-and-drop triage boards, rendered code diffs (inline and side-by-side), and syntax-highlighted code and KQL snippets. | `site/tests/tests/site-plugin.spec.js` - `the What you get section has a Rich content card covering charts, diagrams, boards, diffs, and code (SITE-PLUGIN-16)` |
| SITE-PLUGIN-17 | Feature-card headings render without any CSS-generated greater-than prefix: the `.feature h3::before` pseudo-element has no content, so cards show only their SVG icon when present and the heading text. | `site/tests/tests/site-plugin.spec.js` - `feature card headings render without a generated greater-than prefix (SITE-PLUGIN-17)` |
| SITE-PLUGIN-18 | The commentable-html page top nav includes an Install link that targets `#install` and stays visible on small screens (it does not carry `hide-sm` like Privacy/Changelog). | `site/tests/tests/site-updater-tutorial.spec.js` - `the commentable-html nav has an Install link to the install section (SITE-PLUGIN-18)` |
| SITE-PLUGIN-19 | The "What you get" section includes a card highlighting that comments persist in the browser's `localStorage` and survive a browser restart or machine reboot while iterating, so in-progress review work is not lost. | `site/tests/tests/site-plugin.spec.js` - `the What you get section highlights that comments survive a browser restart (SITE-PLUGIN-19)` |
| SITE-PLUGIN-20 | The "What you get" Round-trip card explains that one Copy all returns every comment at once so the agent makes a single coordinated, coherent edit across all notes, rather than a one-at-a-time pass. | `site/tests/tests/site-plugin.spec.js` - `the What you get Round-trip card explains Copy all returns every comment at once for one coordinated edit (SITE-PLUGIN-20)` |
| SITE-PLUGIN-21 | The "What you get" section includes a card ("No extension, runs everywhere") stating there is no browser extension, plugin, or install to add - the review layer is plain HTML, CSS, and JavaScript inside the file - so it opens in any modern browser on Windows, macOS, and Linux. | `site/tests/tests/site-plugin.spec.js` - `the features section pitches no extension, all-HTML, and cross-platform support (SITE-PLUGIN-21)` |
| SITE-PLUGIN-22 | The "Private by design" section's "You own every copy" card notes that comments are keyed to a stable id baked into the document, so when it is served from one web origin, moving or renaming it there keeps browser-side notes, and an exported file always carries every comment inside it (durability does not depend on any browser store). The claim is qualified to same-origin hosting rather than promising portability across arbitrary file locations. | `site/tests/tests/site-plugin.spec.js` - `the privacy section qualifies comment persistence to same-origin hosting and export (SITE-PLUGIN-22)` |
| SITE-PLUGIN-23 | The plugin page leads with a hero showcase (`#showcase`) placed as the FIRST element right after the hero header and ABOVE both the Install section and the feature grid, so the concrete value proposition leads the page instead of being buried below Install and the feature grid. The showcase content sits in a contained card (`.showcase-card`, a visible background, a real border, rounded corners, and a drop shadow) so it reads as a cohesive unit rather than floating loose on the page. The hero lead itself carries the "code review for your AI's plans and reports" one-liner. The showcase pairs a real, framed product screenshot (`img.showcase-img`, the synced `tutorial/assets/landing-composer.png` with descriptive alt text naming the inline comment window that actually decodes) with the concrete four-step review workflow rendered as numbered steps (Select, Comment inline, Copy all, Reload) that expose explicit ARIA list semantics (`ol.showcase-flow` has `role="list"` with four listitems, the VoiceOver safeguard for the `list-style:none` list) and a "Try the live demo" CTA (`.showcase-cta` to `#demo`). | `site/tests/tests/site-plugin.spec.js` - `the plugin page leads with a review-workflow showcase and a real UI screenshot above the feature grid (SITE-PLUGIN-23)` |
| SITE-PLUGIN-24 | The hero showcase is mobile friendly: on a narrow (phone-width) viewport the screenshot scales down and stays FULLY within the viewport (both its left and right edges are on-screen, not merely narrower than the viewport while shifted off an edge), the `#showcase` block introduces no horizontal overflow, and the card collapses to one column so the screenshot stacks above the review-workflow steps. The card collapses EARLY (by ~900px, so a tablet-width 820px viewport is already stacked), avoiding the cramped two-column squeeze in the tablet band. | `site/tests/tests/site-plugin.spec.js` - `the showcase screenshot scales within a mobile viewport with no horizontal overflow (SITE-PLUGIN-24)` |
| SITE-PLUGIN-25 | The showcase screenshot renders as a small supporting visual (rendered CSS width <= 320px, not a hero-sized image) and stays crisp at up to 2x device-pixel-ratio (the standard retina case): its intrinsic pixel width (`naturalWidth`) is at least twice its rendered CSS width, so at 2x DPR it downscales rather than upscaling (which previously blurred it). | `site/tests/tests/site-plugin.spec.js` - `the showcase screenshot renders small and stays crisp on HiDPI (SITE-PLUGIN-25)` |
| SITE-WHY-01 | The review-loop diagram lives in the "Why commentable-html" section (moved out of "How the review loop works"), which keeps its heading and the three self/peer/reviewer step columns. | `site/tests/tests/site-demo-accessibility.spec.js` - `the review-loop diagram lives in the Why section, not the loop section` |
| SITE-WHY-02 | The "Why commentable-html" section includes a medium-comparison table (chat / Markdown / plain HTML / commentable-html) with commentable-html as the highlighted `compare-hero` row, and references Anthropic's "unreasonable effectiveness of HTML" blog post (opens in a new tab, `rel=noopener`). | `site/tests/tests/site-demo-accessibility.spec.js` - `the Why section presents the medium comparison table and the HTML blog reference` |
| SITE-WHY-03 | On a narrow (mobile) viewport the medium-comparison table stacks each row into a labeled card (column names exposed via `td[data-label]::before`) with no horizontal overflow; the tablet/desktop table layout is unchanged. | `site/tests/tests/site-demo-accessibility.spec.js` - `the medium comparison table stacks without horizontal overflow on a narrow viewport` |
| SITE-WHY-04 | On a narrow (mobile) viewport the review-loop diagram swaps from the wide horizontal SVG (`.loop-fig-h`) to a tall vertical SVG (`.loop-fig-v`, taller than it is wide) so its labels are not cramped against the boxes; the horizontal variant is shown on desktop/tablet and the two live in the single `.loop-figure` container. The vertical variant numbers each transfer with a badge (`.loop-fig-badge`) anchored to its directional arrow (agent-to-you down on the left, you-to-agent up on the right) with `Comment inline` at You. All four steps - including the `Reload and repeat` loop-back - carry a numbered badge (`.loop-fig-badge`, four in total; the old italic `.loop-fig-repeat` caption is gone), so step 4 reads like the other numbered actions on mobile instead of a plain italic caption. Step 4 is title-cased ("Reload and repeat") in both the horizontal and vertical SVG variants. | `site/tests/tests/site-plugin.spec.js` - `the review-loop diagram swaps to a vertical, uncramped layout on a mobile viewport (SITE-WHY-04)` |
| SITE-WHY-05 | On a narrow (mobile) viewport the medium-comparison cards drop the full-cell hero fill and instead tint only each verdict (green good, red bad) and show a good/total score next to each card title (e.g. `3/5`, `5/5`); the tablet/desktop table is unchanged. | `site/tests/tests/site-plugin.spec.js` - `mobile comparison cards color only the verdicts and show a good/total score (SITE-WHY-05)` |
| SITE-WHY-06 | The "Why" section states that Commentable HTML drastically shortens the AI planning and iteration loop. | `site/tests/tests/site-demo-accessibility.spec.js` - `the Why section states commentable-html shortens the AI planning loop` |
| SITE-NAV-01 | Every section with an `id` and a `.section-title` heading (on the hub and plugin pages) has its heading text wrapped in a `.header-anchor` link with `href="#<section-id>"`, so clicking the heading text itself links directly to that section. There is no separate `#` marker glyph; the whole heading is the link. Clicking updates the URL fragment and copies the section's full URL to the clipboard when available; that URL is built from the anchor's own resolved `href`, so it stays a valid absolute URL for any protocol/base (including `file://`, where `location.origin` is the string "null") and preserves the current query string. A heading that already holds an interactive element (`a`/`button`) is skipped, so the enhancement never nests an `<a>` inside an `<a>`. The anchor is a progressive enhancement wired by `site.js`. | `site/tests/tests/site-links-seo.spec.js` - `every section header is a linkable anchor that updates the URL fragment (SITE-NAV-01)`, `section header anchor copies a shareable URL that keeps the query string (SITE-NAV-01)`, `initHeaderAnchors leaves a section whose title already holds a link untouched (SITE-NAV-01)` |
| SITE-NAV-02 | The hub nav "Plugins" entry is a plugin dropdown (mirroring the plugin-page "Marketplace" switcher, left-aligned via `.nav-switcher-start` since it is the first nav item): a `.nav-switcher-trigger` link that still points at `#plugins` (so clicking it scrolls to the section as the old plain link did) and, on hover or keyboard focus, reveals a `.nav-switcher-menu` flyout with one tile per published plugin (friendly display labels), each linking to that plugin's card anchor (`#plugin-<name>`), plus an "All plugins" tile back to `#plugins`. The flyout is hidden by default (a pure-CSS progressive enhancement). The generator lists plugins in manifest order and falls back to a plain `#plugins` link when there are no plugins. Each tile's category sub-label is a Title Case `category_label` mapping (matching the card badges and the plugin-page switcher), never the lowercase manifest slug. | `site/tests/tests/site-hub.spec.js` - `the hub nav 'Plugins' is a dropdown that lists each plugin and scrolls to its card (SITE-NAV-02)`, `the hub nav 'Plugins' dropdown also reveals on keyboard focus (SITE-NAV-02)`; `scripts/test_build_site_data_render.py` - `RenderNavPluginsTests` |
| SITE-WHY-07 | The "Why" section opens by framing HTML as the de-facto standard for planning and reporting with AI agents (rather than claiming agents "increasingly answer with HTML"). | `site/tests/tests/site-demo-accessibility.spec.js` - `the Why section frames HTML as the de-facto standard for AI planning and reporting (SITE-WHY-07)` |
| SITE-WHY-08 | In the medium-comparison table, the Markdown-file row's "Handles a big plan" verdict reads `OK / Need viewer` (a shorter phrasing that fits the mobile card, capital N), not the longer `OK (needs a viewer)`. | `site/tests/tests/site-plugin.spec.js` - `the comparison table's Markdown-file verdict reads 'OK / Need viewer' (SITE-WHY-08)` |
| SITE-WHY-09 | On tablet/desktop (`min-width: 641px`) the medium-comparison table pulls farther into the section-block's side padding (negative side margins) so it is wider than the prose column - its left edge sits left of the intro paragraph and its right edge sits right of it - giving the table more usable width; the narrow (`max-width: 640px`) card layout is unchanged. | `site/tests/tests/site-plugin.spec.js` - `the comparison table breaks a bit out of the section padding for more width (SITE-WHY-09)` |
| SITE-WHY-10 | In the "Why" section's medium list, the "With Commentable HTML" bullet reads "An even richer HTML" (not "The same rich HTML") and wraps the phrase "review in place" in a `mark.hl-comment` styled like the runtime inline comment highlight (a visible, non-transparent amber fill), so the comparison shows what commenting in place looks like. | `site/tests/tests/site-demo-accessibility.spec.js` - `the 'With Commentable HTML' bullet says an even richer HTML and highlights 'review in place' (SITE-WHY-10)` |
| SITE-NAV-02 | A standalone sub-heading can opt into the same header-anchor treatment as a section title by carrying a `data-anchor` attribute and its own `id`; `site.js` wraps its text in a `.header-anchor` link with `href="#<own-id>"` (using the same shared helper as SITE-NAV-01, so the click updates the URL fragment and copies the anchor's resolved absolute URL when a clipboard is available, and a heading already holding an `a`/`button` is skipped). The "Chat, Markdown, HTML - or a tight loop?" sub-heading in the "Why" section (`h3.why-sub#mediums[data-anchor]`) is linkable this way. | `site/tests/tests/site-links-seo.spec.js` - `a standalone data-anchor sub-heading is a linkable anchor to its own id (SITE-NAV-02)` |

## Auto-updater plugin page

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-UPDATER-01 | The auto-updater plugin has its own generated site page at `site/urikan-ai-marketplace-auto-updater/index.html`, a REQUIRED pure build artifact assembled by `build_site_data.py` from its independent `site/pages/urikan-ai-marketplace-auto-updater/index.html` source (carrying the `DO NOT EDIT` banner). A missing source raises a clear `SystemExit`, and a hand-edit to the built page fails `--check` (the same clobber guard as the hub/plugin pages). | `scripts/test_build_site_data_drift.py` - `CheckDriftTests.test_missing_required_auto_updater_page_source_errors_clearly`, `CheckDriftTests.test_check_flags_a_hand_edited_built_auto_updater_page` |
| SITE-UPDATER-02 | The page hero version badge is filled at build time from the plugin's manifest version (a `BEGIN:version` marker region), so it never drifts from the shipped version. | `scripts/test_build_site_data_drift.py` - `CheckDriftTests.test_auto_updater_page_version_badge_matches_manifest` |
| SITE-UPDATER-03 | The auto-updater is registered in `PLUGIN_PAGES`, so its hub card title and Learn more link point at the page, and the page URL is wired into the sitemap, `llms.txt`, and the hub JSON-LD ItemList (its site page URL, not the GitHub source). | `scripts/test_build_site_data_render.py` - `AutoUpdaterPageTests.test_plugin_pages_includes_the_auto_updater`, `AutoUpdaterPageTests.test_render_plugins_links_the_auto_updater_card_to_its_page`, `AutoUpdaterPageTests.test_sitemap_lists_the_auto_updater_page`, `AutoUpdaterPageTests.test_llms_links_the_auto_updater_page`, `AutoUpdaterPageTests.test_jsonld_uses_the_auto_updater_site_page_url`; `site/tests/tests/site-hub.spec.js` - `the auto-updater card navigates to its plugin page (SITE-HUB-06)`, `sitemap.xml is served and lists the hub, plugin, and tutorial pages`, `llms.txt is served and links each plugin and the tutorial` |
| SITE-UPDATER-04 | The auto-updater page presents the full pitch: a hero with the dedicated brand logo (descriptive alt) and a semver version badge, at least four feature cards, and the tabbed dual-agent install block (GitHub Copilot CLI + Claude Code, both tabs present, per SITE-INSTALL-01); it keeps the same tight CSP as the plugin/tutorial pages (`script-src 'self'`, no widget host, no `'unsafe-inline'`) and a self-referencing canonical/OG. | `site/tests/tests/site-updater-tutorial.spec.js` - `the auto-updater page renders its pitch: hero logo, version badge, features, and install (SITE-UPDATER-04)`, `plugin and tutorial pages keep a tight CSP (no widget relaxations)`, `plugin and tutorial pages carry a self-referencing canonical and Open Graph metadata` |
| SITE-UPDATER-05 | The auto-updater page nav brand link stays on the page and a Marketplace link sits immediately after the GitHub link and returns to the hub; the footer links to the plugin's source tree. | `site/tests/tests/site-updater-tutorial.spec.js` - `the auto-updater page keeps the user on the page and links back to the marketplace (SITE-UPDATER-05)` |
| SITE-UPDATER-06 | The commentable-html page Install section references the auto-updater page's install section (relative `../urikan-ai-marketplace-auto-updater/#install`) as a clearly-visible callout card (`.install-updater-cta`, a whole-card link, not plain muted text) that shows the auto-updater plugin icon and explains it keeps the plugin updated on session start. | `site/tests/tests/site-updater-tutorial.spec.js` - `the commentable-html Install section links to the auto-updater install section (SITE-UPDATER-06)` |
| SITE-UPDATER-07 | The auto-updater page "Why auto-update" section links the `urikan-ai-marketplace` name to the marketplace hub page (relative `../`). | `site/tests/tests/site-updater-tutorial.spec.js` - `the auto-updater page Why section links the marketplace name to the hub (SITE-UPDATER-07)` |
| SITE-UPDATER-08 | On the auto-updater page Install section, the PowerShell prerequisite note (`.install-note`) is visibly spaced below the command box (not cramped against it). | `site/tests/tests/site-updater-tutorial.spec.js` - `the auto-updater install note is clearly spaced below the command box (SITE-UPDATER-08)` |
| SITE-UPDATER-09 | The auto-updater page has a generated `#changelog` section (a `BEGIN:changelog` marker region filled by `render_plugin_changelog` from `plugins/urikan-ai-marketplace-auto-updater/CHANGELOG.md`) with a nav Changelog link and a link to the full changelog in source, mirroring the commentable-html page. | `scripts/test_build_site_data_render.py` - `AutoUpdaterPageTests.test_render_plugin_changelog_renders_updater_releases`; `site/tests/tests/site-updater-tutorial.spec.js` - `the auto-updater page renders a changelog section built from its CHANGELOG (SITE-UPDATER-09)` |
| SITE-UPDATER-10 | The auto-updater page "How it works" hook-registration step describes the session-start hook for BOTH agents (naming a Copilot `sessionStart` hook and a Claude Code `SessionStart` hook), not a Copilot-only `hooks.json`, so the dual-agent behavior is represented accurately. | `site/tests/tests/site-updater-tutorial.spec.js` - `the auto-updater page describes the session-start hook for both agents (SITE-UPDATER-10)` |

## multi-duck plugin page

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-MDUCK-01 | The multi-duck plugin has its own generated site page at `site/dist/multi-duck/index.html`, a REQUIRED pure build artifact assembled by `build_site_data.py` from its independent `site/pages/multi-duck/index.html` source (carrying the `DO NOT EDIT` banner). A missing source raises a clear `SystemExit`, and a hand-edit to the built page fails `--check` (the same clobber guard as the hub/plugin/updater pages). | `scripts/test_build_site_data_drift.py` - `CheckDriftTests.test_missing_required_multi_duck_page_source_errors_clearly`, `CheckDriftTests.test_check_flags_a_hand_edited_built_multi_duck_page` |
| SITE-MDUCK-02 | The multi-duck page hero version badge is filled at build time from the plugin's manifest version (a `BEGIN:version` marker region), so it never drifts from the shipped version. | `scripts/test_build_site_data_drift.py` - `CheckDriftTests.test_multi_duck_page_version_badge_matches_manifest` |
| SITE-MDUCK-03 | multi-duck is registered in `PLUGIN_PAGES`, so its hub card title and Learn more link point at the page; the page URL is wired into the sitemap, `llms.txt`, and the hub JSON-LD ItemList (its site page URL, not the GitHub source); and its `#changelog` section is built by `render_plugin_changelog` from `plugins/multi-duck/CHANGELOG.md`. | `scripts/test_build_site_data_render.py` - `MultiDuckPageTests.test_plugin_pages_includes_multi_duck`, `MultiDuckPageTests.test_render_plugins_links_the_multi_duck_card_to_its_page`, `MultiDuckPageTests.test_sitemap_lists_the_multi_duck_page`, `MultiDuckPageTests.test_llms_links_the_multi_duck_page`, `MultiDuckPageTests.test_jsonld_uses_the_multi_duck_site_page_url`, `MultiDuckPageTests.test_render_plugin_changelog_renders_multi_duck_releases` |
| SITE-MDUCK-04 | The multi-duck page recommends running multiple rounds of multi-duck for complex work: a `.callout` note in the "How it works" section explains that each round applies the safe fixes and reshapes the code, so a fresh panel over the updated work almost always surfaces something the previous round could not see, and the number of rounds should scale with the complexity of the change. | `site/tests/tests/site-updater-tutorial.spec.js` - `the multi-duck page recommends running multiple rounds for complex work (SITE-MDUCK-04)` |
| SITE-SWITCH-01 | Each plugin page nav carries a plugin switcher in place of the plain Marketplace link: a `.nav-switcher-trigger` link back to the hub (still the only nav element whose text is "Marketplace") that, on hover or keyboard focus, reveals a `.nav-switcher-menu` flyout of tiles linking to the OTHER plugins (excluding the current page, using friendly display labels that never contain the word "Marketplace") plus an all-plugins link back to the hub; the flyout is hidden by default (a pure-CSS progressive enhancement). The generator excludes the current plugin, orders tiles by manifest order, and returns an empty switcher when no other plugin has a page. | `site/tests/tests/site-plugin.spec.js` - `the commentable-html nav has a Marketplace switcher that reveals tiles to the other plugins (SITE-SWITCH-01)`, `the plugin switcher flyout also reveals on keyboard focus (SITE-SWITCH-01)`; `scripts/test_build_site_data_render.py` - `RenderSwitcherTests.test_excludes_current_and_links_the_other_plugins`, `RenderSwitcherTests.test_uses_display_labels_so_only_the_trigger_says_marketplace`, `RenderSwitcherTests.test_ordered_cmh_multiduck_updater_minus_current`, `RenderSwitcherTests.test_empty_when_no_other_plugin_has_a_page` |
| SITE-SWITCH-02 | Each switcher tile shows a Title Case category subtitle via the `category_label` mapping (`multi-duck` reads "Code and Plan Review", `commentable-html` "Planning and Analysis", the auto-updater "Infrastructure" - never the lowercase slug), and the multi-duck tile name uses the friendly "Multi Duck" display label, not the raw slug. | `scripts/test_build_site_data_render.py` - `RenderSwitcherTests.test_tiles_show_title_case_category_labels`, `RenderSwitcherTests.test_tile_name_uses_friendly_multi_duck_label` |
| SITE-UPDATER-11 | Because the auto-updater excludes itself, the page gives the COMPLETE self-update command for each agent (`copilot plugin update` / `claude plugin update urikan-ai-marketplace-auto-updater@urikan-ai-marketplace`), not a bare or invalid `plugin update`. It lives in the Install-section note (`.install-selfupdate`), not the hero, so the hero pitch stays short. | `site/tests/tests/site-updater-tutorial.spec.js` - `the auto-updater install section gives a complete self-update command for both agents (SITE-UPDATER-11)` |
| SITE-UPDATER-12 | The page states the accurate update-apply timing in the Install-section self-update note (`.install-selfupdate`): the hook fetches releases on session start and Claude Code applies a plugin update on the next restart, so the page does not overstate updates as instantly "already there next time you open". | `site/tests/tests/site-updater-tutorial.spec.js` - `the auto-updater states the accurate Claude update-apply timing (SITE-UPDATER-12)` |
| SITE-UPDATER-13 | Every non-updater plugin page (commentable-html, multi-duck) carries the auto-updater callout card (`.install-updater-cta`) in its Install section, and the card links to the auto-updater page's install section (`../urikan-ai-marketplace-auto-updater/#install`). | `scripts/test_build_site_data_render.py` - `PluginPageStructureTests.test_every_non_updater_plugin_page_has_the_auto_updater_install_cta` |

## Demo slider

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-DEMO-01 | The demo has a single full-screen button that opens in a new tab with `rel="noopener"` and announces the new-tab behavior in its accessible name, and a six-option slider whose Taxi tab is active by default. | `site/tests/tests/site-plugin.spec.js` - `demo has one safe full-screen button and a six-option slider`; `scripts/test_build_site_data_core.py` - `DemoFullscreenTests.test_link_accessible_name_announces_new_tab` |
| SITE-DEMO-02 | Selecting a slider tab switches the iframe `src`, the demo title, and the full-screen target to the chosen report. | `site/tests/tests/site-plugin.spec.js` - `demo slider switches the iframe, title, and full-screen target` |
| SITE-DEMO-03 | The slider is keyboard operable: arrow keys move focus and switch the shown report. | `site/tests/tests/site-demo-accessibility.spec.js` - `demo tabs are keyboard operable (arrow keys switch the shown report)` |
| SITE-DEMO-04 | The slider implements a complete ARIA tabs contract: `role="tabpanel"` panel, `aria-controls`, `aria-selected`, a roving `tabindex`, an `aria-labelledby` that tracks the active tab, and Home/End jump to the first/last tab. | `site/tests/tests/site-demo-accessibility.spec.js` - `demo tabs expose a complete ARIA tabs contract` |
| SITE-DEMO-05 | All six demo reports (Taxi, Community Garden, Triage Board, Visuals Matrix, Checklist, and Notes) load standalone and their commentable-html toolbars mount. | `site/tests/tests/site-demo-accessibility.spec.js` - `all demo reports load and their toolbars mount` |
| SITE-DEMO-06 | The demo frame breaks out of the content column (it is clearly wider than the column), while the "Try it live" heading, its description, and the slider stay aligned with the other section headings in the content column. | `site/tests/tests/site-updater-tutorial.spec.js` - `the demo frame breaks out of the content column while its heading stays in the content column` |
| SITE-DEMO-07 | The demo full-screen button carries an accent-tinted (light-red) background so it reads as the primary action in the frame bar. | `site/tests/tests/site-updater-tutorial.spec.js` - `the full-screen button has a light-red (accent-tinted) background` |
| SITE-DEMO-08 | The full-bleed demo frame keeps a comfortable side buffer (~50px each side) so it never runs too wide or touches the viewport edges. | `site/tests/tests/site-updater-tutorial.spec.js` - `the full-bleed demo frame keeps a comfortable side buffer inside the viewport (SITE-DEMO-08)` |
| SITE-DEMO-09 | The Try it live slider exposes a Checklist tab after the existing demo tabs, uses the ARIA tab attributes expected by the slider, and loads `report-checklist.html` with checklist markup in the iframe. | `site/tests/tests/site-plugin.spec.js` - `demo slider exposes and loads the Checklist report (SITE-DEMO-09)` |
| SITE-DEMO-10 | The Try it live slider exposes a Showcase Deck tab, uses the same ARIA tab contract as the report demos, loads `deck-showcase.html` in the iframe, updates the full-screen target and title, and the framed deck mounts in deck mode with its deck controls visible. | `site/tests/tests/site-plugin.spec.js` - `demo slider exposes and loads the Showcase deck (SITE-DEMO-10)` |

## Copy buttons

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-COPY-01 | The install copy button copies the exact command to the clipboard and shows a `copied` confirmation. | `site/tests/tests/site-install.spec.js` - `install command copy button copies the command and shows feedback` |
| SITE-COPY-02 | When both the async clipboard and `execCommand` fail, the copy button shows a platform-neutral manual-copy hint and a `copy-failed` state instead of claiming success. | `site/tests/tests/site-install.spec.js` - `copy failure gives a platform-neutral manual hint` |
| SITE-COPY-03 | A rapid double click restores the copy button's original label after its feedback window. | `site/tests/tests/site-demo-accessibility.spec.js` - `copy button restores its original label after a rapid double click` |

## Tutorial page

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-TUT-01 | The tutorial page renders from `TUTORIAL.md` with a real heading structure and images whose sources all resolve (no broken images). | `site/tests/tests/site-demo-accessibility.spec.js` - `tutorial page renders from TUTORIAL.md with working images` |
| SITE-TUT-02 | The tutorial page keeps the same tight CSP as the plugin page (no star-widget relaxations, no inline scripts/styles). | `site/tests/tests/site-install.spec.js` - `plugin and tutorial pages keep a tight CSP (no widget relaxations)` |
| SITE-TUT-03 | Clicking a tutorial content image opens a full-size lightbox overlay showing that image, and Escape (or clicking the overlay) closes it. | `site/tests/tests/site-updater-tutorial.spec.js` - `clicking a tutorial image opens a full-size lightbox that Escape closes` |
| SITE-TUT-04 | The tutorial-page nav brand link returns to the commentable-html plugin home, not the hub root. | `site/tests/tests/site-updater-tutorial.spec.js` - `tutorial brand keeps the user in the commentable-html section` |
| SITE-TUT-05 | The tutorial's example-file links resolve to the live demo report under `../demo/` (not a GitHub blob URL), so a reader opens the running report in the browser. | `site/tests/tests/site-updater-tutorial.spec.js` - `tutorial example links open the live demo, not a GitHub blob` |
| SITE-TUT-06 | The tutorial page footer matches the commentable-html plugin page footer: it links to the contributing guide, the feature-request form, the issue chooser, and the commentable-html plugin source tree (plus the author's LinkedIn), instead of a lone `source: TUTORIAL.md` link. | `site/tests/tests/site-updater-tutorial.spec.js` - `the tutorial page footer matches the commentable-html footer links (SITE-TUT-06)` |
| SITE-TUT-07 | Each tutorial content image is keyboard-activatable (`tabindex="0"`, `role="button"`, an accessible name) and Enter/Space opens the lightbox; while the lightbox is open, Tab and Shift+Tab never move focus outside the overlay; and closing it (Escape or the close button) restores focus to the image that opened it. | `site/tests/tests/site-updater-tutorial.spec.js` - `tutorial image lightbox opens via keyboard, traps Tab, and restores focus on close (SITE-TUT-07)` |
| SITE-TUT-08 | The tutorial leads with the value proposition (its first paragraph pitches the interactive review surface handed back to an AI agent, not example-file logistics), and the Help and About walkthrough is moved out of the opening steps to the end, where it reads as the closing reference (it sits in the last third of the section list, order-tolerant so a later appended section does not falsely fail the check). | `site/tests/tests/site-updater-tutorial.spec.js` - `the tutorial leads with the value proposition and ends with the Help and About reference (SITE-TUT-08)` |

## Shared footer

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-FOOTER-01 | The commentable-html plugin page, tutorial page, and auto-updater page share the same footer attribution and navigation structure and link labels: the author's LinkedIn profile, Contribute, Request a feature, File an issue, and Plugin source. The Plugin source link points to the page's owning plugin tree, so the tutorial uses the commentable-html source and the updater uses the auto-updater source. The commentable-html plugin page additionally carries a page-specific rich-content credit (SITE-CREDIT-01) that is not part of this shared structure. | `site/tests/tests/site-plugin.spec.js` - `plugin, tutorial, and updater footers share the same link structure (SITE-FOOTER-01)` |

## Security, portability, and accessibility

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-SEC-01 | The static file server refuses a request whose raw request-target traverses out of `site/` (403), mirroring the deployed Pages boundary the suite validates against. | `site/tests/tests/site-demo-accessibility.spec.js` - `the static test server refuses path traversal out of site/` |
| SITE-SEC-02 | No internal link or asset on any page uses a root-relative path, because a root-relative path would break under the `/ai-marketplace/` project sub-path. | `site/tests/tests/site-links-seo.spec.js` - `no internal link or asset uses a root-relative path (would break the project sub-path)` |
| SITE-SEC-03 | Every internal link and asset on the hub, plugin, and tutorial pages resolves (no broken internal URLs). | `site/tests/tests/site-links-seo.spec.js` - `no broken internal links or assets` |
| SITE-A11Y-01 | Every page exposes a skip-to-content link that targets the `main` region. | `site/tests/tests/site-plugin.spec.js` - `every page exposes a skip-to-content link that targets the main region` |
| SITE-A11Y-02 | Every image on the hub, plugin, and tutorial pages carries non-empty alt text. | `site/tests/tests/site-demo-accessibility.spec.js` - `every image on every page has non-empty alt text` |
| SITE-A11Y-03 | Copy success and failure feedback is exposed through a polite, atomic live region so assistive technology announces the outcome. | `site/tests/tests/site-install.spec.js` - `install command copy button copies the command and shows feedback`, `copy failure gives a platform-neutral manual hint` |
| SITE-A11Y-04 | A plugin card with a page keeps keyboard-focusable title and Learn more links; the title link stretches over the card and the card handler forwards plain body clicks to it, while the install/copy block and foot buttons remain independently clickable. The Learn more button uses the brand accent color and keeps WCAG AA contrast in light and dark. | `site/tests/tests/site-demo-accessibility.spec.js` - `plugin card keeps keyboard links plus the stretched overlay and independent controls (SITE-A11Y-04)`, `the Learn more button keeps AA contrast in light and dark themes`; `scripts/test_build_site_data_render.py` - `RenderPluginsTests.test_plugin_card_title_and_learn_more_link_to_page`, `RenderPluginsTests.test_card_without_page_has_no_learn_more` |
| SITE-A11Y-05 | The portability source chips for CDN and inlined assets meet WCAG AA contrast in the light theme by using readable text color; category color is conveyed by chip accents instead of low-contrast foreground text. | `site/tests/tests/site-plugin.spec.js` - `portability source chips keep AA contrast in the light theme (SITE-A11Y-05)` |

## Theme

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-THEME-01 | The site follows the operating system light or dark color preference with token-based colors that maintain WCAG AA contrast for normal text, links, and primary buttons on page and card backgrounds. | `site/tests/tests/site-plugin.spec.js` - `light and dark themes preserve readable contrast` |

## Discoverability (SEO, social, and AI answer engines)

| Feature id | Behavior | Covering tests |
| --- | --- | --- |
| SITE-SEO-01 | Every page carries a self-referencing `<link rel="canonical">` with its absolute production URL, so the trailing-slash/non-slash duplicates do not split indexing. | `site/tests/tests/site-links-seo.spec.js` - `hub head exposes canonical, Open Graph, and Twitter Card tags`, `plugin and tutorial pages carry a self-referencing canonical and Open Graph metadata` |
| SITE-SEO-02 | Every page emits Open Graph (type, site_name, title, description, url, image + dimensions/alt) and Twitter `summary_large_image` Card tags, so a shared link renders a real preview card on social and chat. | `site/tests/tests/site-links-seo.spec.js` - `hub head exposes canonical, Open Graph, and Twitter Card tags`, `plugin and tutorial pages carry a self-referencing canonical and Open Graph metadata` |
| SITE-SEO-03 | The hub embeds a JSON-LD graph (WebSite + author Person + an ItemList of the plugins as SoftwareApplication) generated from `marketplace.json`, with `<`, `>`, and `&` escaped so manifest text cannot break out of the `<script>` block; the strict `script-src 'self'` CSP does not block the data block. | `scripts/test_build_site_data_render.py` - `JsonLdTests`; `site/tests/tests/site-links-seo.spec.js` - `the hub embeds valid JSON-LD describing the site and its plugins` |
| SITE-SEO-04 | The plugin page embeds a static SoftwareApplication + BreadcrumbList JSON-LD graph (no version field, so it cannot drift). | `site/tests/tests/site-links-seo.spec.js` - `the plugin page embeds SoftwareApplication and BreadcrumbList JSON-LD` |
| SITE-SEO-05 | `build_site_data.py` generates `site/sitemap.xml` listing the hub, each plugin page, and the tutorial; `--check` fails when the committed sitemap is stale. | `scripts/test_build_site_data_render.py` - `SitemapTests`, `WriteOrCheckTests`, `CheckDriftTests`; `site/tests/tests/site-links-seo.spec.js` - `sitemap.xml is served and lists the hub, plugin, and tutorial pages` |
| SITE-SEO-06 | `build_site_data.py` generates `site/llms.txt` (marketplace summary, install commands, per-plugin links and descriptions, and the tutorial link) from the manifest; `--check` fails when it is stale. | `scripts/test_build_site_data_render.py` - `LlmsTests`, `WriteOrCheckTests`, `CheckDriftTests`; `site/tests/tests/site-links-seo.spec.js` - `llms.txt is served and links each plugin and the tutorial` |
| SITE-SEO-07 | A 1200x630 social cover image (`site/dist/assets/og-cover.png`) is served and is referenced by `og:image` on the hub. | `site/tests/tests/site-links-seo.spec.js` - `the og:image cover asset is served as a PNG`, `hub head exposes canonical, Open Graph, and Twitter Card tags` |
| SITE-SEO-08 | The hub H1 reads as continuous, correctly spaced text ("A marketplace of AI plugins for Claude Code and Copilot") so crawlers and screen readers get clean wording despite the styled line break. | `site/tests/tests/site-links-seo.spec.js` - `the hub H1 reads as continuous text with correct word spacing` |
| SITE-SEO-09 | The plugin and tutorial page titles plus social titles use the display name "Commentable HTML" and "Commentable HTML tutorial", while canonical/OG URLs and the plugin page SoftwareApplication JSON-LD `name` keep the literal `commentable-html` identifier. | `site/tests/tests/site-links-seo.spec.js` - `plugin and tutorial metadata use display titles while stable identifiers keep the slug (SITE-SEO-09)` |
| SITE-SEO-10 | The commentable-html plugin and tutorial pages reference a dedicated, plugin-branded 1200x630 social cover (`site/dist/assets/og-commentable-html.png`, served as PNG) in both `og:image` and `twitter:image`, so their link previews are branded to the plugin instead of the generic marketplace cover. | `site/tests/tests/site-links-seo.spec.js` - `the commentable-html plugin and tutorial pages use a dedicated branded social cover (SITE-SEO-10)` |
| SITE-SEO-11 | The marketplace brand displays as "AI Marketplace" everywhere it is shown to a human (the hub header brand and its logo alt text, the hub title/og:title/twitter:title, `og:site_name` on every page, the generated WebSite JSON-LD `name`, the llms.txt heading, and the plugin-page breadcrumb root crumb), while literal identifiers keep the `ai-marketplace` slug (the production/canonical URLs and the GitHub repo link). | `site/tests/tests/site-links-seo.spec.js` - `the marketplace brand and site name display as 'AI Marketplace' while identifiers keep the ai-marketplace slug (SITE-SEO-11)`; `scripts/test_build_site_data_render.py` - `JsonLdTests.test_graph_has_website_person_and_plugin_itemlist`, `LlmsTests.test_contains_summary_install_and_plugin_links` |
| SITE-CREDIT-01 | The commentable-html plugin page footer credits the third-party rich-content libraries it renders with - mermaid (linked to mermaid.js.org) and Chart.js (linked to chartjs.org) - and notes both are MIT, matching the repository README Credits section. | `site/tests/tests/site-plugin.spec.js` - `the plugin page footer credits mermaid and Chart.js (SITE-CREDIT-01)` |

## Coverage gaps

Every behavior in the tables above has an automated test. The `site/dist/assets/styles.css` visual and
responsive rules are exercised indirectly (the theme accent variable and the ARIA/layout tests
render the real stylesheet) but do not have per-breakpoint pixel assertions; commentable-html has
its own responsive suite under `plugins/commentable-html/dev/tests/`. Add a spec row and a covering
test in the same pull request whenever the site grows a new behavior (see AGENTS.md, "Spec-and-test
discipline").
